# 스마트 신호등 프로젝트 — CLAUDE.md

이 문서는 Claude Code가 이 프로젝트에서 작업할 때 참고할 컨텍스트다.
담당자(사용자)의 파트는 **목표 1: 보행자 잔류 감지 기반 신호 연장**이며, 이 문서는 해당 파트를 중심으로 작성한다.

---

## 1. 프로젝트 개요

임베디드 SW 경진대회 출품작 "스마트 신호등". 팀 전체 목표는 두 가지.

- **목표 1 (담당 파트): 보행자 잔류 감지 기반 신호 연장**
  카메라로 횡단보도 영역 내 보행자 존재 여부를 판단하고, 잔여 신호시간이 부족한데 보행자가 남아있으면 신호를 연장한다.
- **목표 2 (팀원 담당): 쓰러짐(이상 상황) 감지 및 자동 신고**
  보행자 자세 변화(서 있음 → 누움)를 감지해 낙상 가능성이 높으면 자동으로 알림/신고 신호를 발생시킨다.

두 목표는 같은 카메라 입력을 공유할 가능성이 높으므로, 영상 캡처/전처리 모듈은 공용으로 설계하고 판단 로직만 분리하는 것을 권장한다. (팀원과 인터페이스 합의 필요)

### 하드웨어 구성

- 라즈베리파이 (비전 처리 + 판단 로직)
- USB 웹캠
- 아두이노 (신호 제어 담당)
- 신호등 LED
- 7세그먼트 (잔여 시간 표시로 추정 — 정확한 용도는 팀 내 확인 필요)

라즈베리파이가 영상 분석 및 의사결정을, 아두이노가 실제 LED/7세그먼트 구동을 맡는 역할 분담으로 보인다. 라즈베리파이 ↔ 아두이노 통신은 시리얼(UART/USB) 사용을 가정한다. **이 통신 프로토콜(메시지 포맷, 보드레이트 등)은 팀원과 반드시 사전 합의할 것.**

---

## 2. 담당 파트 상세: 보행자 잔류 감지 기반 신호 연장

### 2.1 카메라 조건

- 카메라는 **보행자 신호등이 아니라 자동차 신호등에 부착** — 위에서 아래를 내려다보는 탑뷰(bird's-eye에 가까운 downward-angle) 구도.
- 이 각도 특성상 사람의 전신 실루엣보다 머리~어깨 위주로 보일 가능성이 높음 → 사람 검출 모델/파라미터 튜닝 시 고려.
- 정확한 설치 각도·높이는 실측 후 캘리브레이션 필요 (하드웨어팀과 협의).

### 2.2 판단 방식: 구역(Zone) 기반

카메라 각도상 좌표→실거리 변환이 어려우므로, 정밀한 속도/위치 추정보다 **구역 기반 잔류 판단**을 기본 전략으로 한다.

- 횡단보도 영역을 하나 이상의 zone(폴리곤 또는 사각형 ROI)으로 정의.
- 각 프레임에서 사람 검출 결과의 bounding box(또는 발 위치 추정점)가 zone 내부에 있는지 판정.
- zone 내 사람이 일정 프레임/시간 이상 연속 검출되면 "잔류"로 판단.
- (보조 옵션) 프레임 간 위치 변화로 대략적인 이동 속도/방향을 계산해, 신호 종료 전 zone을 벗어날 수 있는지 보조 판단에 활용 가능. 단, 탑뷰 원근 왜곡으로 정밀한 속도 계산은 한계가 있음을 감안.

### 2.3 신호 연장 로직

- 조건: (남은 신호 시간이 임계값 이하) AND (zone 내 보행자 잔류로 판단됨) → 신호 연장 트리거.
- 연장 단위: **5초씩** 반복 연장.
- **상한 설정 필수**: 차량 신호와의 충돌 방지를 위해 최대 연장 누적 시간을 상한값으로 고정. (구체적인 초 단위 상한은 팀/도로교통 관련 규정 참고 후 결정 — 임의로 확정하지 말 것)
- 연장 중에도 매 사이클(또는 1초 단위)로 zone 잔류 여부를 재평가해, 보행자가 사라지면 불필요한 추가 연장을 하지 않도록 한다.

### 2.4 예외 규칙: 교통약자 우선 연장

- 휠체어 또는 지팡이가 검출되면 **우선 연장** 규칙 적용.
- 우선 연장의 구체적 차등(예: 연장 단위를 5초가 아닌 더 크게, 또는 상한을 별도로 높게 설정)은 아직 미정 — 설계 시 결정 필요.
- 휠체어/지팡이 검출은 별도 클래스로 사람 검출 모델에 포함하거나(커스텀 데이터셋/파인튜닝 필요), 사람 검출 후 보조 규칙(예: bounding box 종횡비, 보행 속도 패턴)으로 근사 판단하는 방법이 있음. **정확도가 낮을 수 있으므로 실제 채택 전 팀 내 검증 필요.**

### 2.5 목표 2(쓰러짐 감지)와의 인터페이스 고려

목표 2는 bounding box 종횡비 변화 또는 포즈 기반 자세 변화로 낙상을 판단한다고 명시됨. 담당 파트의 사람 검출 파이프라인(검출 모델, bounding box 좌표)을 목표 2 팀원과 공유할 수 있는 구조로 설계하면 중복 개발을 줄일 수 있음. (팀원과 인터페이스 스펙 합의 필요 — 이 문서에서 임의로 확정하지 않음)

---

## 3. 개발 프로세스 (제안 — 팀/일정에 맞게 조정)

1. **요구사항 확정**: 신호 연장 조건, 5초 단위, 상한값, 우선 연장 규칙의 구체적 파라미터를 팀 회의로 확정.
2. **테스트 영상 확보**: 실제 설치 각도와 유사한 탑뷰 각도로 촬영한 영상/이미지 확보 (실제 현장이 어렵다면 유사 각도로 사전 촬영).
3. **사람 검출 모델 선정**: 실시간성과 라즈베리파이 자원 제약을 고려해 경량 모델 후보를 비교 검토 (예: YOLO 계열 경량 버전, MediaPipe 등 — 각 모델의 라즈베리파이 실측 FPS는 반드시 직접 벤치마크할 것. 특정 모델의 속도/정확도를 사전에 확정된 수치로 가정하지 말 것).
4. **Zone 정의 도구 제작**: 카메라 프레임 위에 zone 좌표를 지정/저장할 수 있는 간단한 캘리브레이션 스크립트 작성.
5. **잔류 판단 로직 구현**: zone 내 검출 지속 프레임 카운트 기반 로직 구현 및 단위 테스트.
6. **신호 연장 상태 머신 구현**: 남은 시간, 연장 누적, 상한 체크를 포함한 상태 머신 설계 및 구현.
7. **교통약자 예외 규칙 구현 및 검증**: 휠체어/지팡이 판단 로직 추가, 오탐/미탐 케이스 별도 테스트.
8. **라즈베리파이 ↔ 아두이노 통신 구현**: 신호 연장 신호를 아두이노로 전달하는 프로토콜 구현 (프로토콜 스펙은 팀 합의 후 확정).
9. **하드웨어 통합 테스트**: LED/7세그먼트 실제 반응 확인.
10. **현장/모의 환경 테스트 및 파라미터 튜닝**: 임계값, 프레임 카운트 기준, 상한값 등 실측 기반 보정.

---

## 4. 폴더 구조 (제안)

```
claude/
├── CLAUDE.md
├── src/
│   ├── capture/          # 카메라 입력 처리
│   ├── detection/        # 사람/휠체어/지팡이 검출 모델 래퍼
│   ├── zone/              # zone 정의, 잔류 판단 로직
│   ├── signal_extend/     # 신호 연장 상태 머신 (5초 단위, 상한, 예외 규칙)
│   └── serial_comm/       # 라즈베리파이-아두이노 통신
├── arduino/               # 아두이노 펌웨어 코드
├── tools/                 # zone 캘리브레이션, 테스트 스크립트 등
├── tests/                 # 단위/통합 테스트
├── data/                  # 테스트 영상, 캘리브레이션 데이터
└── docs/                  # 설계 문서, 팀 합의 사항 기록
```

실제 폴더명/구조는 팀 컨벤션에 맞춰 자유롭게 수정.

---

## 5. Claude Code 작업 시 유의사항

- 하드웨어 사양(초 단위 상한값, 통신 프로토콜, 모델 선택 등) 중 **아직 팀과 확정되지 않은 값은 임의로 확정하지 말고, 코드 내 상수/설정 파일로 분리**하여 나중에 쉽게 바꿀 수 있게 할 것.
- 모델 성능(FPS, 정확도 등) 관련 수치는 실측 없이 추정치를 사실처럼 기록하지 말 것 — 반드시 벤치마크 결과를 근거로 남길 것.
- 라즈베리파이 자원 제약(CPU/메모리)을 고려해 실시간 처리가 가능한 경량 접근을 우선 검토.
- 커밋/변경 시 어떤 파라미터를 왜 그 값으로 정했는지 `docs/` 또는 커밋 메시지에 근거를 남길 것 (추후 대회 발표 자료 작성 시 활용).

---

## 6. 팀과 논의가 필요한 미확정 사항

- 신호 연장 누적 상한 (초 단위)
- 교통약자 우선 연장의 구체적 규칙 (연장 폭 차등 여부, 별도 상한 여부)
- 라즈베리파이-아두이노 통신 프로토콜 스펙
- 목표 2(쓰러짐 감지) 파이프라인과 사람 검출 모델/좌표계 공유 여부
- 7세그먼트가 표시할 정보(잔여 시간인지 다른 정보인지)
