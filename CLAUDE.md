# 스마트 신호등 프로젝트 — CLAUDE.md

이 문서는 Claude Code가 이 프로젝트에서 작업할 때 참고할 컨텍스트다.
담당자(사용자)의 파트는 **목표 1: 보행자 잔류 감지 기반 신호 연장**이며, 이 문서는 해당 파트를 중심으로 작성한다.

---

## 1. 프로젝트 개요

"스마트 신호등" 만들기. 팀 전체 목표는 두 가지.

- **목표 1 (담당 파트): 보행자 잔류 감지 기반 신호 연장**
  카메라로 횡단보도 영역 내 보행자 존재 여부를 판단하고, 잔여 신호시간이 부족한데 보행자가 남아있으면 신호를 연장한다.
- **목표 2 (팀원 담당): 쓰러짐(이상 상황) 감지 및 자동 신고**
  보행자 자세 변화(서 있음 → 누움)를 감지해 낙상 가능성이 높으면 자동으로 알림/신고 신호를 발생시킨다.

두 목표는 같은 카메라 입력을 공유할 가능성이 높으므로, 영상 캡처/전처리 모듈은 공용으로 설계하고 판단 로직만 분리하는 것을 권장한다. (팀원과 인터페이스 합의 필요)

### 하드웨어 구성

- 라즈베리파이 (비전 처리 + 판단 로직)
- CSI 라즈베리파이 카메라
- **ESP32 (제어부)** — 신호 상태/타이머 관리
- **아두이노 우노 (구동부)** — LED·7세그먼트 실제 구동
- 신호등 LED
- 7세그먼트 (잔여 시간 표시)
- 횡단보도 모형
- 사람 모형

역할 분담: 라즈베리파이가 영상 분석 및 의사결정, ESP32가 신호 상태/잔여 시간 제어, 아두이노 우노가 LED/7세그먼트 구동을 맡는다. 파이 ↔ 제어부 통신은 시리얼(UART/USB) 사용을 가정한다. **이 통신 프로토콜(메시지 포맷, 보드레이트 등)은 팀원과 반드시 사전 합의할 것.**

> ⚠️ **제어부/구동부 구성(ESP32 + 우노)은 잠정안이며 변경될 수 있다.** 따라서 `src/serial_comm.py`는
> 상대 보드에 의존하지 않는 인터페이스만 유지하고, 포트·보드레이트·메시지 포맷은 전부 config로 분리한다.
> 비전 쪽(검출·구역 판정·속도 추정)은 제어부 구성과 무관하게 독립적으로 완성할 수 있어야 한다.

---

## 2. 담당 파트 상세: 보행자 잔류 감지 기반 신호 연장

### 2.1 카메라 조건

- 카메라는 **보행자 신호등에 부착**되어 횡단보도를 **사선(oblique)으로 비추는** 구도다.
  위에서 수직으로 내려다보는 탑뷰가 아니다.
- 사선 구도이므로 사람의 **전신 실루엣이 대체로 보인다** → bounding box 아래쪽이 지면에 닿는 발 위치에 대응한다.
- 이 때문에 위치 판정 기준점은 bbox **중심점이 아니라 하단 모서리의 중심(`BoundingBox.foot_point`)**을 쓴다.
  중심점은 사람 키만큼 위로 떠 있어서, 사선 각도에서는 실제 서 있는 지점보다 카메라 쪽으로 당겨진 위치로 잘못 판정된다.
- 사선 구도의 대가로 **원근 왜곡**이 생긴다 — 카메라에 가까운 쪽 1픽셀과 먼 쪽 1픽셀이 나타내는 실거리가 다르다.
  이건 2.2의 호모그래피로 보정한다.
- 정확한 설치 각도·높이는 실측 후 캘리브레이션 필요 (하드웨어팀과 협의).

### 2.2 판단 방식: 5구역(Zone) 기반 위치 판정 + 평면 좌표 변환

횡단보도를 **걷는 방향을 따라 위치 순서대로 5개 구역**으로 나눈다.

```
진입 끝 ┃ 1 │ 2 │ 3 │ 4 │ 5 ┃ 반대 끝
        └───┴───┴───┴───┴───┘
1, 5 = 양 끝(시작) 구역   2, 4 = 끝과 중앙 사이   3 = 정중앙
```

- 캘리브레이션 때 횡단보도 사각형의 네 꼭짓점을 찍으면(`tools/zone_calibrator.py`), 걷는 방향으로 자동 5등분한다(`CrosswalkZones.from_quad`). 구역 수는 `config.CROSSWALK_ZONE_COUNT`.
- 각 프레임에서 사람 검출 bounding box의 **발 위치(foot_point = 하단 모서리 중심)**가 몇 번 구역에 있는지 판정(`CrosswalkZones.locate` → 1~5 또는 None).
- 검출 흔들림 대비: 횡단보도 안에서 `config.ZONE_RESIDENCY_FRAMES` 이상 연속 검출돼야 "확정 보행자"로 본다(`CrosswalkOccupancy`).

**호모그래피(평면 좌표 변환) — `src/ground_plane.py`**

구역 판정만으로는 해상도가 5단계뿐이라 속도를 낼 수 없다. 그래서 **픽셀 좌표 → 횡단보도 평면 좌표(cm)**
변환을 따로 둔다. 보행자의 발은 지면(평면) 위에 있으므로, 평면 하나에 대한 사영변환(호모그래피)이면 충분하다.

- 별도의 체스보드 카메라 캘리브레이션은 **필요 없다.** 이미 `zone_calibrator`에서 찍는 **네 꼭짓점**에
  **횡단보도 모형의 실제 가로/세로 치수(cm)**만 추가로 입력하면 `cv2.getPerspectiveTransform`으로 변환 행렬이 나온다.
- 평면 좌표계 규약: **`y` = 걷는 방향(0 → 세로 길이 cm), `x` = 폭 방향(0 → 가로 폭 cm)**.
- 치수(`CROSSWALK_REAL_WIDTH_CM` / `CROSSWALK_REAL_LENGTH_CM`)가 미입력이면 `GroundPlane`은 만들어지지 않고
  `CrosswalkZones.ground_plane`이 `None`이 된다. 이 경우 **구역 판정은 그대로 동작**하고 속도만 픽셀 단위로 떨어진다.
- 카메라를 옮기면 호모그래피가 무효가 된다 → **카메라를 고정한 뒤 캘리브레이션할 것.**

### 2.3 속도 추정 — `src/speed.py`

발 위치의 평면 좌표가 시간에 따라 변하는 정도로 보행 속도를 추정한다.

- `SpeedEstimator`가 트랙 ID별로 `(시각, 평면 좌표)` 히스토리를 `config.SPEED_WINDOW_SEC` 길이만큼 유지하고,
  윈도우 양 끝의 변위 ÷ 경과 시간으로 속도를 낸다. **한 프레임 차분은 검출 지터에 그대로 흔들리므로 쓰지 않는다.**
- 반환값 `TrackSpeed`:
  - `speed` — 평면상 전체 속력
  - `crossing_speed` — **걷는 방향(y축) 성분만**. 좌우 흔들림이 섞이지 않아 "얼마나 빨리 건너는가"에 직접 대응한다.
  - `direction` — `+1`(끝 방향) / `-1`(시작 방향) / `0`
  - `unit` — `"cm/s"` 또는 `"px/s"`
- **예상 통과 시간** = (진행 방향 기준 남은 거리) ÷ `crossing_speed`.
- 호모그래피가 없으면(치수 미입력) 속도는 `px/s`로 떨어지고 예상 통과 시간은 `None`을 반환한다.
  원근 보정되지 않은 픽셀 속도는 화면 위/아래 위치에 따라 값이 달라져 절대 비교가 무의미하므로,
  **실거리인 척 변환하지 않고 명시적으로 포기한다.**
- 시각(timestamp)은 인자로 주입받는다 → 카메라 없이 단위 테스트 가능.

> **현재 속도는 계산·노출까지만 하고 연장 시간 결정에는 쓰지 않는다.** 연장은 2.4의 구역별 규칙 그대로다.
> 실측으로 속도 추정이 충분히 안정적인지 검증한 뒤 반영 여부를 결정한다.

### 2.4 신호 연장 로직: 위치 기반 차등 연장

핵심 아이디어 — 보행자가 **가운데에 있을수록 아직 더 건너야 하므로 더 길게 연장**한다.

- 조건: (남은 신호 시간이 `REMAINING_TIME_THRESHOLD_SEC` 이하) AND (횡단보도 안에 확정 보행자 존재).
- 연장 시간은 보행자가 있는 **구역별로 차등**(`config.ZONE_EXTENSION_SEC`):
  - 3번(정중앙): 가장 길게 (담당자 예시 5초 — **임의값이라 config에 None**)
  - 2·4번(중앙-끝 사이): 중간 (예시 3초 — **None**)
  - 1·5번(양 끝): **연장 안 함(0)** ← 이건 설계상 확정 규칙
  - 여러 명이면 가장 많이 필요한 사람(연장 시간이 가장 큰 구역)에 맞춘다.
- **상한 설정 필수**: 차량 신호 충돌 방지를 위해 누적 연장 상한(`MAX_TOTAL_EXTENSION_SEC`) 고정. (초 단위 상한은 팀/규정 확인 후 결정 — 임의 확정 금지)
- 매 사이클 재평가: 남은 시간이 충분해지거나 보행자가 사라지면 연장하지 않는다. (연장 후 잔여 시간이 늘어 임계값 위로 올라가면 자연히 추가 연장이 멈춘다.)

> 구역별 초 단위 값(2·3·4번)은 담당자가 예로 든 임의값이므로 코드에는 `None`으로 두고 주석을 달아 두었다. 실측/팀 확정 후 채운다. `None`인 구역에 보행자가 있으면 `NotImplementedError`로 멈춰 임의값으로 동작하지 않는다.

### 2.5 예외 규칙: 교통약자 우선 연장 — **현재 보류**

**보류 상태.** 지금 쓰는 검출 모델은 COCO 사전학습 `yolov8n.pt`이고, COCO에는 휠체어/목발/지팡이 클래스가
없다(2.6 참고). 별도 데이터셋으로 파인튜닝하기 전까지는 교통약자를 검출할 수단이 없으므로,
`config.MOBILITY_AID_LABELS`를 **빈 튜플**로 두어 `priority_mode`가 항상 `False`가 되게 한다.

아래 설계와 코드 인터페이스는 나중에 파인튜닝 모델이 생기면 그대로 쓸 수 있도록 남겨둔다.

- 하나의 YOLO 모델이 사람과 함께 휠체어/목발/지팡이(`config.MOBILITY_AID_LABELS`)를 한 번의 추론으로 검출한다
  (2단계 재검출 없음 → 라즈베리파이 자원 절약). 교통약자 클래스가 하나라도 잡히면 우선 연장 모드(`priority_mode`).
- 우선 연장은 **처음부터 시간을 넉넉하게** 주는 것이 목표다. 이는 두 갈래로 구현한다.
  1. 제어부(ESP32)가 기본 녹색 시간을 더 길게 세팅 (파이 → 제어부 priority 플래그 전달, `SerialComm.send_extend_signal(..., priority=True)`).
  2. 파이 쪽 연장 규칙도 별도 맵/상한으로 차등 (`config.PRIORITY_ZONE_EXTENSION_SEC`, `PRIORITY_MAX_TOTAL_EXTENSION_SEC`).
- 우선 연장의 구체적 차등값은 아직 미정 → config에 `None`. 미정이면 state machine이 일반 규칙으로 안전하게 대체 동작한다(교통약자를 일반 보행자보다 불리하게 대하지는 않음).
- 재개할 때 검증할 것: 지팡이(cane)는 사선·저해상도 조건에서 매우 작게 보여 검출률이 낮을 수 있음.

### 2.6 검출 모델: COCO 사전학습 yolov8n

실측 확인 결과 **COCO 사전학습 `yolov8n.pt`가 사람 모형을 `person`으로 검출하며, 신뢰도는 80%대**로
관측되었다. 따라서 **추가 학습 없이 사전학습 가중치를 그대로 사용**한다.

- `config.DETECTION_MODEL_PATH = "yolov8n.pt"`, `config.PEDESTRIAN_LABEL = "person"` (COCO 클래스명).
- `config.DETECTION_CONFIDENCE_THRESHOLD = 0.5` — 위 80%대 관측 대비 여유가 있는 값. 오탐 발생 시 재조정.
- 트랙 ID는 `YOLO.track(persist=True)`가 부여한다. 속도 추정과 `CrosswalkOccupancy`가 이 ID에 의존한다.
- 라즈베리파이 실측 FPS는 아직 벤치마크 전 — **수치를 추정으로 기록하지 말 것.** 느리면 `ncnn`/`tflite` 변환,
  입력 해상도 축소를 검토한다.

### 2.7 목표 2(쓰러짐 감지)와의 인터페이스 고려

목표 2는 bounding box 종횡비 변화 또는 포즈 기반 자세 변화로 낙상을 판단. 담당 파트의 사람 검출 파이프라인(검출 모델, bounding box 좌표)을 목표 2 팀원과 공유할 수 있는 구조로 설계하면 중복 개발을 줄일 수 있음. (팀원과 인터페이스 스펙 합의 필요 — 이 문서에서 임의로 확정하지 않음)

---

## 3. 개발 프로세스 (제안 — 팀/일정에 맞게 조정)

1. **요구사항 확정**: 신호 연장 조건, 구역별 연장 시간(2·3·4번), 임계값, 상한값, 우선 연장 규칙을 팀 회의로 확정.
2. **테스트 영상 확보**: 실제 설치 각도와 유사한 **사선 각도**로 촬영한 영상/이미지 확보 (실제 현장이 어렵다면 유사 각도로 사전 촬영).
3. ~~**사람 검출 모델 선정**~~ → **완료**: COCO 사전학습 `yolov8n.pt`를 그대로 사용(2.6). 다만 **라즈베리파이 실측 FPS 벤치마크는 아직 미완** — 특정 모델의 속도를 사전에 확정된 수치로 가정하지 말 것.
4. **Zone 정의 도구 제작** → **완료**(`tools/zone_calibrator.py`). 남은 것: **카메라를 최종 위치에 고정한 뒤** 네 꼭짓점 클릭 + **횡단보도 모형 실측 치수(cm) 입력**.
5. **잔류 판단 로직 구현**: zone 내 검출 지속 프레임 카운트 기반 로직 구현 및 단위 테스트.
6. **속도 추정 검증**: 사람 모형을 일정 속도로 이동시키며 `SpeedEstimator` 출력이 실제 속도와 얼마나 맞는지 확인. 오차가 크면 `SPEED_WINDOW_SEC` 조정.
7. **신호 연장 상태 머신 구현**: 남은 시간, 연장 누적, 상한 체크를 포함한 상태 머신 설계 및 구현.
8. ~~**교통약자 예외 규칙**~~ → **보류**(2.5). 파인튜닝 데이터셋 확보 후 재개.
9. **라즈베리파이 ↔ 제어부 통신 구현**: 연장 신호를 제어부로 전달하는 프로토콜 구현 (프로토콜 스펙은 팀 합의 후 확정).
10. **하드웨어 통합 테스트**: LED/7세그먼트 실제 반응 확인.
11. **현장/모의 환경 테스트 및 파라미터 튜닝**: 임계값, 프레임 카운트 기준, 상한값 등 실측 기반 보정.

---

## 4. 폴더 구조

중첩 패키지를 없애고 각 역할을 **단일 모듈 파일**로 평탄화했다(파일이 너무 많아 헷갈리던 문제 정리).

```
config/config.py        # 전역 설정 (미확정 값은 None + 주석)
main.py                 # 실행 진입점
src/
├── capture.py          # 카메라 입력 (USB 웹캠 / 영상 파일 / 라즈베리파이 스트림 URL)
├── detection.py        # 단일 YOLO 검출 래퍼 (yolov8n, person + (보류)교통약자)
├── ground_plane.py     # 호모그래피: 픽셀 좌표 -> 횡단보도 평면 좌표(cm)
├── zone.py             # Zone, 5구역 분할(CrosswalkZones), 점유 추적(CrosswalkOccupancy)
├── speed.py            # 트랙별 속도/진행 방향/예상 통과 시간 추정
├── signal_extend.py    # 위치 기반 신호 연장 상태 머신
├── serial_comm.py      # 라즈베리파이-제어부 시리얼 통신
└── pipeline.py         # 전체 흐름을 잇는 파이프라인
arduino/                # 아두이노 펌웨어 코드
tools/                  # zone_calibrator, pi_camera_server, manual_camera_person_check
tests/                  # 단위 테스트
data/                   # zone 설정(zone_config.json), 테스트 영상 (git 추적 제외)
docs/                   # 설계 결정 로그, 팀 합의 사항
```

데이터 흐름은 `src/pipeline.py`의 `SignalExtensionPipeline`이 담당:

```
카메라 프레임
  → PersonDetector.detect            (yolov8n, track_id 부여)
  → box.foot_point()                 (bbox 하단 모서리 중심 = 지면 접점)
  ├→ CrosswalkZones.locate           → CrosswalkOccupancy   → 확정 보행자/점유 구역
  └→ GroundPlane.to_ground           → SpeedEstimator.update → 속도/방향/예상 통과 시간
  → SignalExtensionStateMachine.evaluate  (점유 구역만 사용 — 속도는 아직 미반영)
  → SerialComm.send_extend_signal
```

---

## 5. Claude Code 작업 시 유의사항

- 하드웨어 사양(초 단위 상한값, 통신 프로토콜, 모델 선택 등) 중 **아직 팀과 확정되지 않은 값은 임의로 확정하지 말고, 코드 내 상수/설정 파일로 분리**하여 나중에 쉽게 바꿀 수 있게 할 것.
- 모델 성능(FPS, 정확도 등) 관련 수치는 실측 없이 추정치를 사실처럼 기록하지 말 것 — 반드시 벤치마크 결과를 근거로 남길 것.
- 라즈베리파이 자원 제약(CPU/메모리)을 고려해 실시간 처리가 가능한 경량 접근을 우선 검토.
- 커밋/변경 시 어떤 파라미터를 왜 그 값으로 정했는지 `docs/` 또는 커밋 메시지에 근거를 남길 것 (추후 대회 발표 자료 작성 시 활용).

---

## 6. 팀과 논의가 필요한 미확정 사항

- 구역별 연장 시간 (`ZONE_EXTENSION_SEC`의 2·3·4번 초 단위 값 — 현재 임의값이라 None)
- 연장 트리거 임계값 (`REMAINING_TIME_THRESHOLD_SEC`)
- 신호 연장 누적 상한 (`MAX_TOTAL_EXTENSION_SEC`, 초 단위)
- **횡단보도 모형 실측 치수** (`CROSSWALK_REAL_WIDTH_CM`, `CROSSWALK_REAL_LENGTH_CM`) — 속도를 cm/s로 내려면 필수
- 잔류 확정 프레임 수 (`ZONE_RESIDENCY_FRAMES`, 실측 FPS 기반)
- 라즈베리파이-제어부(ESP32) 통신 프로토콜 스펙 (포트/보드레이트/메시지 포맷, 잔여 시간 읽기 포함)
- **제어부/구동부 보드 구성 확정** (현재 ESP32 + 아두이노 우노는 잠정안)
- 속도 추정값을 연장 시간 결정에 반영할지 여부 (현재는 계산·노출만)
- 목표 2(쓰러짐 감지) 파이프라인과 사람 검출 모델/좌표계 공유 여부

**보류 (지금 결정 안 함)**

- 교통약자 우선 연장 규칙 (`PRIORITY_ZONE_EXTENSION_SEC`, `PRIORITY_MAX_TOTAL_EXTENSION_SEC`)
  — 검출 수단부터 없음(2.5). 파인튜닝 데이터셋 확보 후 재논의.

**확정됨**

- 검출 모델/가중치: COCO 사전학습 `yolov8n.pt`, `person` 클래스 (2.6). 라즈베리파이 FPS 벤치마크만 미완.

---

## 7. 라즈베리파이 연결 / 실행 방법

"파이에 연결만 하면 VSCode 실행으로 바로 되는가?"에 대한 답 — **완전히 자동은 아니다.** 두 가지
배치 중 하나를 골라야 한다. 검출 모델 가중치(`DETECTION_MODEL_PATH`)는 `yolov8n.pt`로 확정됐지만,
**시리얼 프로토콜(`SERIAL_*`)**이 config에 채워져 있어야 `main.py`의 전체 루프가 돈다. 미정이면
의도적으로 `NotImplementedError`로 멈춘다.

> 비전 파트만 확인하고 싶다면 시리얼 없이 `tools/manual_camera_person_check.py`로 검출·구역·속도를
> 눈으로 볼 수 있다.

### 배치 A — 파이에서 직접 실행 (가장 단순)

CSI 카메라(또는 USB 웹캠)를 파이에 연결하고, 파이 위에서 코드를 실행한다. VSCode Remote-SSH로 파이에 접속해 실행 가능.

- `config.CAMERA_SOURCE = 0`. `cv2.VideoCapture(0)`가 그대로 프레임을 읽는다.
  (CSI 카메라가 `/dev/video0`으로 안 잡히면 `libcamera`/`picamera2` 경유가 필요할 수 있다 — 실기에서 확인할 것.)
- 제어부 보드도 보통 파이의 USB에 꽂으므로 `SERIAL_PORT`는 `/dev/ttyACM0` 또는 `/dev/ttyUSB0`.
- 단점: 파이 CPU에서 YOLO 추론이 무겁다 → `ncnn`·`tflite` 변환, 해상도 축소 등 필요(실측 벤치마크).

### 배치 B — 카메라는 파이, 무거운 추론은 PC

파이는 프레임만 내보내고, YOLO 추론은 성능 좋은 PC에서 돌린다.

1. 파이에서: `python tools/pi_camera_server.py --source 0 --port 8000` (프레임을 MJPEG로 송출).
2. PC에서: `config.CAMERA_SOURCE = "http://<파이IP>:8000/"` 로 설정.
   `CameraCapture`(=`cv2.VideoCapture(URL)`)가 네트워크 스트림을 그대로 프레임으로 받는다.
3. 제어부 보드를 PC에 꽂으면 시리얼도 PC에서, 파이에 꽂으면 파이에서 별도 중계가 필요하다(팀 배치에 따라 결정).

> 주의: **호모그래피는 해상도에 종속된다.** 캘리브레이션할 때와 운영할 때의 프레임 해상도가 다르면
> zone 좌표와 변환 행렬이 전부 어긋난다. 배치 A/B를 바꾸거나 스트림 해상도를 바꾸면 재캘리브레이션할 것.

> 요약: `src/capture.py`는 `CAMERA_SOURCE`가 정수/파일경로/URL 무엇이든 처리하므로, 프레임을
> 가져오는 코드는 이미 준비돼 있다. 배치 B용 파이 송출 스크립트(`tools/pi_camera_server.py`)도 포함했다.
> 같은 로컬 네트워크(공유기 안)에서만 쓰는 평문 스트림임에 유의.
