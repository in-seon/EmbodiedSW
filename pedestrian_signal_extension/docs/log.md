# 작업 로그

이 프로젝트의 문서는 각각 다른 질문에 답한다.

| 문서 | 답하는 질문 |
|---|---|
| `CLAUDE.md` | 왜 이렇게 만드는가 (배경·설계 의도) |
| `decisions.md` | 왜 이 숫자인가 (파라미터 근거) |
| `bugfix_log.md` | 무엇이 잘못됐고 어떻게 고쳤는가 (상세) |
| `implementation.md` | 무엇이 어떤 순서로 실행되는가 |
| **이 문서** | **무엇이 언제 왜 바뀌었는가** (시간순 흐름) |

새 항목은 위에 추가한다.

---

## 2026-08-26: 신호 연장 방식을 세 번 갈아엎었다

하루 만에 프로토콜이 세 번 바뀌었다. 매번 앞 단계에서 발견한 문제가 다음 단계를
불러왔으므로, 그 경위를 남긴다. **최종안이 처음부터 나올 수 있었던 것이 아니라,
앞의 두 안을 만들어 보고 나서야 보인 것들이 있었다.**

### 1차 — 판단을 제어부로 이관 (`EXTEND <초> <ETA|->`)

**계기**: 파이 쪽 `SignalExtensionStateMachine`이 222줄이었다. 잔여 녹색 시간을 시리얼로
읽어 와서 "지금 연장할까"까지 판단하는 구조라, 임계값 비교·누적 상한·엣지 트리거(무장
플래그)가 전부 필요했다.

**깨달은 것**: "남은 시간이 5초 미만인가"는 7세그먼트를 직접 세는 아두이노만 답할 수 있고,
"이 사람이 몇 번 구역에 있나"는 영상을 보는 파이만 답할 수 있다. **각자 자기만 아는 것을
판단하도록 나누면** 파이 쪽 상태 기계가 통째로 사라진다.

**결과**: `signal_extend.py` 222줄 → 130줄. "한 번 상한을 찍으면 영구 CAPPED" 위험
(`bugfix_log.md` B 항목)도 함께 사라졌다 — 누적을 파이가 갖지 않으니 그 버그가 성립하지 않는다.

### 2차 — 한 필드로 (`EXTEND <필요시간>`)

**계기**: 두 필드(`<구역값> <ETA>`)가 아두이노 파싱을 복잡하게 했다. 태그(`EXTEND ETA 7.2` /
`EXTEND ZONE 5`)로 구분하는 안도 검토했으나 채택하지 않았다.

**정한 것**: 숫자 하나로 하되 의미를 **"연장할 초"가 아니라 "앞으로 필요한 시간"**으로
고정했다. 파이는 잔여 시간을 모르므로 뺄셈을 할 수 없고, 뺄셈이 아두이노에서 일어나려면
보내는 값이 '연장량'이 아니라 '필요 시간'이어야 하기 때문이다.

**여기서 사고가 났다**: 의미를 바꾸면서 구역값(3초/5초)을 그대로 뒀더니, 실제 연장이
2·4번 구역에서 **0초**가 됐다. `3 x 1.25 - 5 = -1.25` 라 부족분이 음수였다.
`USE_SPEED_FOR_EXTENSION`이 `False`여서 **항상 그 경로**였는데도 에러가 나지 않아
시뮬레이션을 돌려보고서야 발견했다.

### 3차 — `ZONE <진척도>` (최종)

**계기**: 팀에서 "구역 번호만 보내고 아두이노가 기준표와 비교하자"는 안이 나왔다.
기본 녹색 10초·5구역이면 각 구역 도착 시 9/7/5/3/1초가 남아 있어야 정상이고,
그보다 적으면 뒤처진 것이니 조금씩 연장한다.

**이 방식이 없애준 것**:

| 사라진 것 | 왜 중요했나 |
|---|---|
| ETA 계산과 최소 프레임률(4fps) | 파이 실측이 **3 FPS**였다 — 이전 안에서는 ETA가 아예 안 나올 위험이 있었다 |
| 안전계수 `ETA_SAFETY_MARGIN` | 기준표와의 차이가 곧 부족분이다 |
| **교통약자 검출** | 느린 사람은 기준 대비 지연으로 자동 검출되어 더 연장받는다 |

기준표가 정상 보행 속도를 암묵적으로 담고 있어서 파이가 속도를 잴 필요가 없어졌다.
그리고 매 구역에서 지연을 다시 재는 **피드백 제어**라 오차가 누적되지 않는다 —
저 프레임률로 구역 하나를 건너뛰어도 다음 구역에서 자동으로 만회된다.

**함께 만든 것 — `CrossingProgress`**: 물리 구역 번호는 좌표계 기준이라 진입 방향에 따라
의미가 뒤집힌다. 반대편에서 온 사람의 '2번'은 '거의 다 건넜음'인데 보정 없이는 '대부분
남았음'으로 읽힌다. 방향은 **첫 검출 구역**으로 판별한다(속도가 아니라 위치 이력이라
프레임률 요구가 없다). 중앙에서 처음 보이면 중앙값으로 간주하고 이후 이동으로 확정한다.

**검증**(시뮬레이션): 정상 속도·정시 진입 = 연장 0초 / 5초 늦게 진입 = 6초 /
느린 보행자(구역당 3초) = 4초. 셋 다 건넜다.

---

## 2026-08-26: 실측으로 확인한 것들

추정으로 적어두면 나중에 근거가 되지 않으므로, 실제로 재서 얻은 값만 남긴다.

### 추론이 프레임 시간의 99.6%다

```
YOLO 추론        :  82.39 ms
쓰러짐 판정+배선  :   0.35 ms      <- 우리 코드 전부
비율             :     237 : 1
```

**이 숫자가 여러 결정의 근거가 됐다.** 우리 코드를 아무리 최적화해도 FPS는 소수점 아래에서만
바뀐다. 반대로 두 파이프라인이 각자 `detect()`를 부르면 FPS가 그대로 반토막 난다 —
`CombinedPipeline`이 추론 결과를 나눠 주는 구조로 만든 이유다.

### `DETECTION_IMGSZ`가 가장 효과적인 튜닝 지점

| imgsz | 추론 시간 | 배수 | 검출 |
|---|---|---|---|
| 640 | 97.4 ms | 1.0× | 4명 |
| 480 | 48.4 ms | 2.0× | 3명 ← 한 명 놓침 |
| **320** | **34.5 ms** | **2.8×** | 4명 |
| 256 | 33.0 ms | 3.0× | 4명 |

320이 가장 좋은 지점이다. **재캘리브레이션이 필요 없다** — YOLO가 내부적으로만 축소하고
박스는 원본 프레임 좌표로 돌아오기 때문이다. `CAMERA_RESOLUTION`을 줄이는 것과 결정적으로
다른 점이고, 그래서 파이가 느릴 때 가장 먼저 건드릴 값이다.

### 서 있는 사람의 몸통 각도는 1~3°

`fall_angle_deg` 기본값 50°와 충분히 분리된다. 정면 사진 기준이므로 사선 구도에서는
다시 재야 하지만, 30°까지 낮출 필요는 없어 보인다.

### ultralytics가 추론 시간을 자동으로 재준다

`results[0].speed` → `{'preprocess': 1.42, 'inference': 49.29, 'postprocess': 1.56}` (ms).
루프 전체 FPS는 카메라 대기·그리기가 섞인 값이라 "무엇이 느린지"를 못 가르는데,
이 값은 추론만 떼어 준다. **첫 호출은 워밍업이라 2배 느리므로 측정에서 제외할 것.**

---

## 2026-08-26: 경로가 실행 디렉터리에 종속돼 있었다

같은 원인의 문제가 세 곳에서 나왔고, 셋 다 **에러 메시지가 경로를 탓하지 않아** 찾기 어려웠다.

1. **`ZONE_CONFIG_PATH`가 상대 경로** — `tools/`에서 캘리브레이터를 돌리면 `tools/data/`가
   생기고, 프로젝트 루트에서 `main.py`를 돌리면 그걸 못 찾는다.
2. **절대 경로로 바꾼 뒤 앞 슬래시 사고** — `Path(root) / "/data"` 는 pathlib이 앞 경로를
   통째로 버려서 `C:\data`(파이에서는 `/data`, 파일시스템 최상위)를 가리킨다. 파이에서는
   권한 오류로 저장 자체가 실패한다.
3. **모델 가중치도 같은 문제** — 리포 루트에서 실행하면 `yolov8n-pose.pt`를 못 찾아
   6.5MB를 다시 내려받고 실행 위치마다 사본이 생겼다. 파인튜닝 가중치는 내려받을 곳도 없어
   그냥 실패한다.

**대응**: 셋 다 프로젝트 루트 기준 절대 경로로 바꾸고, `tests/test_config_paths.py`로 고정했다.
가중치는 "있으면 절대 경로, 없으면 이름 그대로"라 최초 클론 시 자동 다운로드는 살아 있다.

---

## 2026-08-25: 한 프레임만 놓쳐도 누적이 리셋됐다

**증상**: 검출이 한 프레임이라도 끊기면 그 사람의 잔류 카운트가 0으로 돌아가고 속도
히스토리가 통째로 지워졌다. YOLO는 가림·모션블러·저조도에서 한두 프레임씩 놓치는 것이
정상인데, 그때마다 확정 보행자 자격을 잃었다.

**왜 위험한가**: 에러가 나지 않는다. 현장에서는 "왜 가끔 연장이 안 되지"로만 보인다.

**대응**: `TRACK_GRACE_FRAMES = 2` 도입. 유예 동안 카운트는 **동결**한다 — 안 보이는 동안은
잔류의 증거가 없으므로 올려주지 않되, 이미 쌓은 것을 뺏지도 않는다.
단 **'구역 밖으로 나감'은 유예 대상이 아니다**(즉시 리셋). "안 보임"은 정보가 없는 상태지만
"밖으로 나감"은 확실한 정보라, 둘을 같이 취급하면 이미 떠난 사람 때문에 차를 세우게 된다.

**단위를 '초'가 아니라 '프레임 수'로 한 이유**: 잔류 확정 자체가 프레임 수 기준이라 같은
단위여야 섞이지 않는다. `FALL_CONFIG`의 `track_grace_sec`이 '초 + 프레임 배수' 적응형인
것은 그쪽 누적(`fall_confirm_sec`)이 시간 기준이라서다.

---

## 2026-08-25: 시리얼 통신과 쓰러짐 → 부저 배선

**한 보드가 부저·LED·7세그먼트를 다 담당**하기로 해서 시리얼 채널도 하나로 갔다.
프레이밍은 개행으로 끝나는 ASCII 한 줄로 확정.

**수신을 `poll()` 한 곳으로 모은 이유**: 한 채널로 여러 종류의 줄이 섞여 온다.
"필요할 때 `readline()` 한 번" 방식이면 명령이 늘어날 때 서로의 응답을 잡아먹는다
(잔여 시간을 읽으려다 `OK ALERT`를 먹어버리는 식).

**전송 정책 — 변화 시 + 1초 하트비트**:
- 매 프레임 보내면 아두이노의 64바이트 수신 버퍼가 넘치고, 응답까지 하면
  그 송신 시간(13바이트당 13.5ms)에 아두이노 루프가 묶인다
- 변화 시에만 보내면 그 한 줄을 놓쳤을 때 아두이노가 **영영 옛 상태로 남는다**
- 하트비트가 있어야 아두이노가 "N초간 조용함 = 파이가 죽음"을 워치독으로 쓸 수 있다

**`--mode fall` 신설**: 쓰러짐 감지는 제어부 프로토콜에 의존하지 않으므로 먼저 완결할 수
있었다. 카메라 → YOLO → 쓰러짐 판정 → 부저까지 한 줄로 돈다.

---

## 하드웨어 쪽에서 겪은 것

| 증상 | 원인 |
|---|---|
| 도구가 시작조차 못 함 | `MOBILITY_AID_MODEL_PATH`에 `.pt` 확장자 누락 |
| 포트는 보이는데 부저가 안 움 | 능동/수동 부저 불일치 가능성. `arduino/buzzer_only_test/`로 분리 진단 |
| (예상) 파이에서 포트 못 엶 | `dialout` 그룹 미가입 / 아두이노 IDE 시리얼 모니터가 포트 점유 |

**부품이 스피커라면 핀에 직결하지 말 것.** 8Ω을 5V에 직결하면 600mA 이상이 흐르는데
아두이노 핀 정격은 20mA다. 멀티미터로 저항을 재면 갈린다(4~8Ω 스피커 / 16~42Ω 수동 부저 /
수백 Ω~ 능동 부저).

---

## 축척 분석 — 모형 치수를 정하면서

모형 횡단보도가 폭 10cm × 길이 30cm면 실제 4차선 횡단보도(400cm × 1200cm)의 **1:40 축소판**이다.

중요한 것은 거리 축척이 아니라 **시간 축척**이다. 연장 시간은 거리가 아니라 시간이고,
`시간 축척 = 거리 축척 / 속도 축척` 이므로 모형 보행자를 **2.5 cm/s**(실제 보행속도의 1/40)로
움직이면 시간 축척이 1:1이 되어 국내 운영 사례의 초 단위 값을 그대로 쓸 수 있다.

    검산: 30cm / 2.5cm/s = 12초  ==  실제 12m / 1.0m/s = 12초

**모터 속도를 맞추는 것이 연장 초를 바꾸는 것보다 낫다.** 값을 임의로 축소하면 근거 자료
(광주시 5초, 서울시 3~6초, 강진군 5~10초)와의 연결이 끊어진다.

---

## 2026-08-27 — 모형 보행자 모터 + 쓰러짐 연출 서보

### 문제: 모터의 '켜는 사람'과 '끄는 사람'이 다르다

아두이노 핀이 모자라 스텝모터를 **파이 GPIO**에 연결했다. 그런데 판단에 필요한 두 사실이
서로 다른 보드에 있다:

    보행 녹색이 방금 시작됐다     -> 신호를 소유한 아두이노만 안다
    모형이 횡단보도를 다 건넜다   -> 영상을 보는 파이만 안다

프로토콜 설계 내내 지켜 온 원칙("각자 자기만 아는 것을 판단한다")을 그대로 적용해,
**기동은 아두이노(`START`), 정지는 파이**로 나눴다. 시리얼 방향이 처음으로 역전된 지점이다.

### 함정: '보행자가 없으면 정지'는 시작하자마자 멈춘다

가장 자연스러운 구현이 틀렸다.

    START 수신 -> 모형은 아직 횡단보도 밖 -> 확정 보행자 0명 -> "나갔다" -> 즉시 정지

`START`가 오는 순간은 녹색이 막 켜진 시점이라 모형이 아직 진입 전이다. 정지 조건은
"없음"이 아니라 **"있었다가 없어짐"**이어야 한다. `MotorGate._seen` 래치가 그것이다.

이 함정을 테스트로 고정했고(`test_does_not_stop_before_seeing_anyone`),
`tools/manual_motor_check.py --gate`가 GPIO 없이 그 시퀀스를 눈으로 보여준다.

파생 함정도 하나 더 있었다: **래치를 다음 사이클에 비우지 않으면 첫 횡단만 되고
두 번째부터 안 된다.** 시연에서 제일 곤란한 형태라 별도 테스트로 막았다.

### 안전 타임아웃을 넣은 이유

정지 조건이 "있었다가 없어짐"이므로, 모형이 탈선하거나 검출이 계속 실패하면 그 조건이
**영영 오지 않고 모터가 계속 돈다.** 소프트웨어였다면 조용한 실패로 끝났겠지만 물리
장치라 모터가 탄다. `MOTOR_MAX_RUN_SEC = 40`은 정상 동작에서 절대 발동하지 않아야 하는 값이다.

### 스텝 루프를 별도 스레드로 뺀 이유

스텝 간격 5ms vs 비전 루프 100~330ms(3~10 FPS). **두 자릿수 차이라 같은 스레드에 둘 수 없다.**
비전 루프 안에서 스텝을 밟으면 한 프레임에 한 스텝이라 모터가 20배 느려진다.
반대로 서보는 각도만 지정하면 하드웨어 PWM이 유지하므로 스레드가 필요 없다.

### `--mode fall`의 의미를 바꿨다

기존 `--mode fall`은 "쓰러짐만 감지"였는데, **"전체를 돌리되 서보로 모형을 눕힌다"**로
바꿨다. 손으로 넘어뜨리면 프레임에 사람 손이 들어와 검출을 교란하기 때문이다.
zone 없이 돌던 기존 동작은 `--mode fall-only`로 남겼다 — 캘리브레이션 전에 카메라만
놓고 확인하는 용도가 실제로 유용했다.

서보를 **파이프라인 밖에** 둔 것이 설계상 핵심이다. 안에 넣으면 "쓰러짐을 감지했으니
쓰러뜨린다"는 순환이 되고, 시연 도구가 판정 경로에 섞인다. 지금은 파이프라인이 서보의
존재를 모르는 채로 판정하므로, 시연이 실제 검출 경로를 그대로 통과하는 진짜 확인이 된다.

### 놓쳤던 것: 모터가 둘인데 서로 맞물려야 한다

처음 구현에서 두 모터를 **독립적으로** 만들었다. 스텝모터는 START부터 횡단 완료까지
계속 돌고, 서보는 정해진 시각에 눕힌다 — 각각은 맞는데 **같이 돌리면 넘어진 모형이
그대로 끌려간다.** 데모 영상에서 가장 이상하게 보일 장면이다.

지적을 받고 나서야 보였다. 각 부품이 자기 일을 정확히 하는 것과 **부품들이 같이 있을 때
말이 되는 것**은 다른 문제다.

고치면서 '멈춤'을 두 종류로 나눠야 했다:

| | 뜻 | `_seen` 래치 | 재개 조건 |
|---|---|---|---|
| `stop()` | 이번 횡단이 끝났다 | 지운다 | 다음 `START` |
| `pause()` | 같은 횡단 도중 잠깐 | **유지한다** | `resume()` |

`stop()`으로 대충 처리했다면 일어난 뒤의 이동이 새 사이클로 오해되고, 그 시점엔 이미
확정 보행자가 있으므로 **곧바로 '횡단 완료'로 판정돼 영영 안 움직인다.**

파생 함정 둘:
- **누우면 검출이 빠진다.** bbox 모양이 급변해 트래커가 ID를 새로 달거나 놓친다.
  일시정지 중에는 정지 판단 자체를 건너뛰게 했다.
- **연출로 세워 둔 시간이 안전 타임아웃에 잡히면 안 된다.** `resume()`이 멈춰 있던
  만큼 시계를 뒤로 민다.

서보 스케줄러가 문자열 대신 `FallEvent(kind=...)`를 돌려주도록 바꿨다. 호출자가 이
시점에 스텝모터를 조작해야 하는데, 안내 문구를 파싱하게 두면 **문구를 고치는 순간
조용히 동작이 바뀐다.**

### 테스트를 쓰다가 스스로 고친 것

`test_motor_stops_when_model_finishes_crossing`을 "박스가 사라짐"으로 썼더니 실패했다.
`TRACK_GRACE_FRAMES = 2` 때문에 한 프레임 미검출로는 보행자가 지워지지 않는다 —
**코드가 맞고 테스트가 비현실적이었다.** 실제 모형은 프레임에서 사라지는 게 아니라
횡단보도 밖에 계속 보이고, 구역 이탈은 유예 없이 즉시 리셋된다.
테스트를 실제 경로로 고치고, 유예 동작 자체도 별도 테스트로 고정했다
(`test_one_dropped_frame_does_not_stop_the_motor`).

---

## 2026-08-29 — 모터·서보를 파이에서 들어냄 (별도 보드로 이관)

모형 구동을 다른 보드가 맡기로 하면서 파이 쪽 모터/서보 코드를 전부 제거했다.
`src/motor.py`, `src/servo.py`, `tools/manual_motor_check.py`와 각 테스트, config의
`MOTOR_*`/`SERVO_*` 블록, 파이프라인의 구동 배선, `main.py`의 `--no-motor`·`--fall-after`·
`--fall-hold`가 함께 사라졌다.

**시리얼이 다시 한 방향이 됐다.** `START`/`STOP` 수신 경로(`take_commands`, 명령 큐)를
지우면서, 아두이노 → 파이 방향에 남은 것은 연결 확인용 `READY`/`PONG` 뿐이다.
`--mode fall`도 원래 뜻('쓰러짐 감지만')으로 돌아왔다 — 서보 연출 모드가 없어졌으므로
`fall-only`라는 이름을 따로 둘 이유가 없다.

지운 코드의 수명은 이틀이었지만, 그동안 알아낸 것 중 **하드웨어와 무관한 부분은 남는다**:

- 정지 조건은 '없음'이 아니라 '있었다가 없어짐'이어야 한다 (구동을 맡는 보드에서도 같다)
- 물리 장치에는 안전 타임아웃이 필요하다 — 조용한 실패로 끝나지 않는다
- 넘어진 모형을 계속 끌고 가면 안 되고, 그 멈춤은 '정지'가 아니라 '일시정지'다

이 세 가지는 새 보드를 맡는 쪽에 그대로 전달할 가치가 있다. 커밋 `ab4064e`/`8d28903`에
구현과 테스트가 남아 있으니 필요하면 꺼내 쓸 수 있다.

---

## 2026-08-30 — 교통약자 별도 검출 제거

`MobilityAidDetector`와 `MOBILITY_AID_*` 설정 5개, `BoundingBox.is_mobility_aid()`,
`FrameResult.priority_mode`/`mobility_aid_count`, 도구의 `--aid-*` 옵션을 전부 지웠다.

**왜 필요 없어졌나** — 구역 기준표가 정상 보행 속도를 담고 있어서다:

    기준[n] = 기본녹색 x (1 - (2n-1)/(2N))     # 10초/5구역 -> 9,7,5,3,1

느린 사람은 기준 대비 지연이 크게 잡혀 자동으로 더 연장받는다. **"휠체어인가"를 알
필요 없이 "느린가"만 보면 되고, 그건 이 방식이 이미 잰다.** 애초에 공개 휠체어
데이터셋은 실제 사람/휠체어 사진으로 학습돼 있어 축소 모형은 못 잡을 가능성이 높았다.

**왜 남겨 두지 않았나** — 8월 26일 개편 때는 "계측용으로 남긴다"고 했다. 그런데 남은
`MOBILITY_AID_MODEL_PATH = None`, `MOBILITY_AID_LABELS = (...TODO...)` 가 계약서와
구현 문서에서 계속 **"아직 채워야 할 값"**처럼 보였다. 쓰이지 않는 설정이 미결 항목
목록에 남아 있으면, 볼 때마다 "이걸 해야 하나"를 다시 판단하게 된다. 그 비용이
계측 가치보다 컸다.

`src/speed.py`는 그대로 뒀다 — 화면에 실제로 표시되고 발표 자료에 쓸 수 있는 값이라,
같은 기준(쓰이는가)으로 봐도 남길 이유가 있다.

**부수 효과**: 스킵되던 테스트 2개가 사라졌다. `MOBILITY_AID_MODEL_PATH`가 None이라
건너뛰던 경로 검증이었는데, 설정 자체가 없어졌으니 검증할 것도 없다.

---

## 배운 것 (회고)

**① 조용히 틀리는 버그가 가장 비쌌다.** 이번에 찾은 것들이 거의 다 그랬다 — 경로 종속,
config 체인 대입(`PRIORITY_ZONE_EXTENSION_SEC = ZONE_EXTENSION_SEC = {...}` 때문에 일반
보행자에게도 교통약자 값이 나가고 있었다), 의미 변경 후 남은 옛 값. 전부 에러가 안 나서
시뮬레이션이나 테스트를 돌려보고서야 보였다.

**② 값을 바꾸면 그 값의 '의미'도 확인해야 한다.** 2차 개편에서 `EXTEND` 뒤 숫자의 의미를
'연장량'에서 '필요 시간'으로 바꿨는데, 구역값 3초/5초는 그대로 뒀다. 단위가 섞여
연장이 0초가 됐다. **의미를 바꾼 뒤에는 그 자리에 있던 값이 여전히 유효한지 다시 봐야 한다.**

**③ 테스트가 자기 값을 소유해야 한다.** `config`를 읽는 테스트는 config를 만질 때마다
깨진다. 실제로 그렇게 깨진 덕분에 체인 대입 버그를 찾긴 했지만, 그건 운이었다.

**④ 실측이 설계를 바꿨다.** 파이 3 FPS라는 숫자가 없었다면 ETA 기반 설계를 그대로 갔을
것이고, 현장에서 "속도가 왜 안 나오지"를 겪었을 것이다.

**⑤ "각자 자기만 아는 것을 판단한다"가 계속 답이었다.** 연장 프로토콜(3회 개편), 부저
끄는 시점, 모터 기동/정지까지 전부 같은 원칙 한 줄로 갈렸다. 판단을 **정보를 가진 쪽**에
두면 주고받는 메시지가 저절로 줄어든다.

**⑦ 부품이 각자 맞는 것과 함께 있을 때 맞는 것은 다르다.** 모터 두 개를 각각
올바르게 만들어 놓고, 둘이 동시에 도는 장면을 상상하지 않았다. 넘어진 모형이 끌려가는
그림은 코드를 아무리 봐도 안 보이고 **데모를 머릿속에서 재생해야** 보인다.

**⑥ 물리 장치는 조용한 실패가 없다 — 대신 타는 실패가 있다.** 소프트웨어라면
"정지 조건이 안 와서 루프가 계속 도는" 버그가 로그 한 줄로 끝나지만, 모터는 손상된다.
안전 타임아웃과 `finally`의 `close()`는 선택이 아니었다.
