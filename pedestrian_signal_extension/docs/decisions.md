# 파라미터 결정 로그

CLAUDE.md 5장: 파라미터를 정할 때 근거를 남겨 대회 발표 자료에 활용한다.
새 항목은 아래 형식으로 위에 추가.

## 템플릿

### YYYY-MM-DD: 항목명

- **결정값**:
- **근거**:
- **참고 자료/벤치마크**:
- **관련 config 값**:

---

### 2026-08-26 (2차): 프로토콜을 `ZONE <진척도>`로 — 속도 추정 의존성 제거

- **결정값**: 파이는 `NORMAL` / `ZONE <1..N>` / `FALL` 세 상태만 보낸다.
  `ZONE` 뒤의 숫자는 **가장 덜 건넌 사람의 진척도**(진입 방향으로 보정한 구역 번호)다.
  아두이노가 구역별 '정상 도착 기준 잔여시간' 표와 비교해 뒤처진 만큼 2초씩 연장한다.

- **근거**:

  **① 기준표가 정상 보행 속도를 암묵적으로 담는다.**

        기준[n] = 기본녹색 x (1 - (2n-1)/(2N))     # 10초/5구역 -> 9,7,5,3,1

  "정상 속도로 걸었다면 n번째 구역에 도착했을 때 이만큼 남아 있어야 한다"이므로,
  실제 잔여시간과의 차이가 곧 그 사람의 지연이다. **파이가 속도를 재서 넘길 필요가 없다.**

  그 결과 아래가 전부 불필요해졌다:
    - ETA 계산과 최소 프레임률 4fps 요구 -> **파이가 3fps여도 정상 동작한다**
      (실측 3fps였으므로 이전 설계에서는 ETA가 아예 안 나올 위험이 있었다)
    - 안전계수 `ETA_SAFETY_MARGIN`
    - **교통약자 검출** — 느린 사람은 기준 대비 지연으로 자동 검출되어 더 연장받는다.
      "휠체어인가"를 알 필요 없이 "느린가"만 보면 되고, 그건 이 방식이 자동으로 잰다.

  **② 피드백 제어라 오차가 누적되지 않는다.** 매 구역에서 지연을 다시 재므로, 저 fps에서
  구역 하나를 건너뛰어도 다음 구역에서 누적 지연이 그대로 잡혀 자동으로 만회된다.
  늦게 진입해 뛰어가는 사람은 다음 구역에서 지연이 줄어 연장도 자연히 줄어든다.
  검증(시뮬레이션): 정상 속도·정시 진입 = 연장 0초 / 5초 늦게 진입 = 6초 /
  느린 보행자(구역당 3초) = 4초. 셋 다 건넜다.

  **③ 최솟값 하나만 보내면 된다.** 기준표가 진척도에 대해 단조 감소하고 모든 사람이 같은
  잔여시간을 보므로, `부족분 = 기준[n] - 잔여`는 진척도가 작을수록 항상 크다. 가장 작은
  진척도의 사람이 항상 가장 부족하다 — 수학적으로 보장된다.

- **진입 방향 보정 (`zone.CrossingProgress`)**:
  물리 구역 번호는 좌표계 기준이라 진입 방향에 따라 의미가 뒤집힌다. 반대편에서 들어온
  사람의 '2번'은 '거의 다 건넜음'인데, 보정하지 않으면 '대부분 남았음'으로 읽혀 엉뚱한
  사람이 최솟값을 차지한다.

  방향은 **첫 검출 구역**으로 판별한다(속도가 아니다 — 프레임률 요구가 없고 사람이 멈춰
  있어도 유지된다). 중앙에서 처음 보이면 방향을 모르므로 **중앙값으로 간주**하고, 이후
  구역이 바뀌면 그 방향으로 확정한다. 3->4든 3->2든 진척도 4로 수렴한다.

  오판하면(중간에 ID가 재발급되면) 진척도가 **실제보다 작게** 나오고, 작은 진척도는
  '덜 왔다'이므로 **더 연장하는** 쪽으로 틀린다 — 안전한 방향의 오차다.

- **1회 2초씩 나눠 주는 이유**: 크게 한 번 주면 보행자가 곧바로 횡단을 마쳐도 되돌릴 수
  없다. **낭비의 상한이 곧 1회 연장량**이다. 1초로 하면 파이 하트비트(1초)와 주기가 겹쳐
  매 하트비트마다 연장이 붙고 7세그먼트가 깜빡이는 것처럼 보인다.

- **함께 발견해 고친 것**: config에서 `PRIORITY_ZONE_EXTENSION_SEC = ZONE_EXTENSION_SEC = {...}`
  체인 대입 때문에 **일반 보행자에게도 교통약자 값이 나가고 있었다.** 에러 없이 조용히
  틀리는 종류라 회귀 테스트를 추가했다(`test_general_and_priority_maps_are_independent`).

- **남긴 것**: `src/speed.py`(속도·ETA)와 `MobilityAidDetector`는 지우지 않았다.
  연장 판단에는 쓰이지 않지만 도구 화면과 발표 자료용 계측으로 계속 쓴다.

- **참고**: `tests/test_zone.py`(진척도 8개), `tests/test_signal_extend.py`(9개),
  `tests/test_pipeline.py`(21개). 아두이노 계약은 `docs/team_interface.md`.

- **관련 config**: `BASE_GREEN_SEC`·`ZONE_ARRIVAL_BASE_SEC`·`MAX_SINGLE_EXTEND_SEC`(신설,
  전부 아두이노가 쓰는 값), `ZONE_EXTENSION_SEC`·`PRIORITY_*`·`REMAINING_TIME_THRESHOLD_SEC`·
  `USE_SPEED_FOR_EXTENSION`·`ETA_SAFETY_MARGIN`(제거).


### 2026-08-26: 신호 연장 판단을 제어부(아두이노)로 이관 — 프로토콜을 3상태로 단순화

- **결정값**: 파이는 `NORMAL` / `EXTEND <초> <ETA|->` / `FALL` 세 상태만 보낸다.
  잔여 시간 임계값·누적 상한·엣지 트리거·사이클 리셋은 전부 아두이노가 소유한다.

- **근거**:

  **① 각자 자기만 아는 것을 판단하게 나눴다.**
  "남은 시간이 5초 미만인가"는 7세그먼트를 직접 카운트다운하는 아두이노만 답할 수 있고,
  "이 사람이 몇 번 구역에 있나"는 영상을 보는 파이만 답할 수 있다. 이전 구조는 파이가
  잔여 시간을 시리얼로 **읽어 와서** 판단했는데, 자기가 소유하지 않은 값으로 상태를
  추론해야 해서 엣지 트리거·무장 플래그 같은 장치가 필요했다.

  **② 그 결과 파이 쪽 코드가 줄었다.** `signal_extend.py`가 222줄 -> 130줄.
  `SignalExtensionStateMachine`(누적·상한·무장·`SignalState` enum)이 통째로 사라지고
  상태 없는 `ZoneExtensionRule`만 남았다. `begin_new_cycle()`도 필요 없어졌고,
  그와 함께 **"한 번 상한을 찍으면 영구 CAPPED"** 위험(bugfix_log B 항목)도 사라졌다.

  **③ ETA는 넘겨서 아두이노가 뺄셈하게 했다.** 속도 보정식이
  `연장 = min(구역값, ceil(ETA x 1.25 - 잔여시간))` 인데 파이는 잔여시간을 모른다.
  처음엔 "4상태로 가면 속도를 못 쓴다"고 판단했으나, **보낼 것이 속도가 아니라 ETA**임을
  정리하니 해결됐다 — 뺄셈을 두 숫자를 다 가진 쪽으로 옮기면 된다.
  구역값이 상한으로 남으므로 **속도는 연장을 깎기만 하고 늘리지 않는다**는 성질은 그대로다.

  **④ 전송은 "변화 시 + 1초 하트비트"다.** 매 프레임 보내면 9600bps에서 아두이노의
  64바이트 수신 버퍼가 넘치고, 아두이노가 응답까지 하면 그 송신 시간(13바이트당 13.5ms)에
  루프가 묶인다. 반대로 변화 시에만 보내면 그 한 줄을 놓쳤을 때 아두이노가 영영 옛 상태로
  남는다. 1초 재전송이 둘을 모두 막는다. **ETA는 변화 판정에서 제외**했다 — 걷는 동안
  매 프레임 변하므로 넣으면 결국 매 프레임 전송이 된다.

- **함께 고친 것 — 정지한 사람의 ETA 폭주**:
  `estimated_crossing_time_sec()`이 `crossing_speed <= 0`만 걸러서, 서 있는 사람의
  부동소수점 오차가 거대한 ETA로 증폭됐다(실측 관측 `ETA=10351225.3s`). 파이 안에서만
  쓸 때는 구역값이 상한이라 무해했지만 이제 그 값이 아두이노로 나가므로,
  `SPEED_STOPPED_THRESHOLD_CM_S = 0.5`(모형 설계 속도 2.5cm/s의 20%) 미만은 정지로 본다.

- **함께 고친 것 — 추론 1회 공유**:
  `--mode full`에서 쓰러짐과 신호 연장이 각자 `detect()`를 부르면 추론이 2배가 된다.
  실측상 추론이 프레임 시간의 **99.6%**(82.4ms vs 0.35ms)라 FPS가 그대로 반토막 난다.
  `process_boxes()`를 분리해 `CombinedPipeline`이 한 번 검출한 결과를 둘에 나눠 준다.
  포즈 모델을 고른 이유(2026-08-19 항목)가 여기서 실제로 지켜진다.

- **참고 자료/벤치마크**: `tests/test_signal_extend.py`(16), `tests/test_pipeline.py`(22),
  `tests/test_serial_comm.py`(24). 아두이노 쪽 계약은 `docs/team_interface.md`.

- **관련 config 값**: `SERIAL_STATE_HEARTBEAT_SEC`(신설), `SPEED_STOPPED_THRESHOLD_CM_S`(신설),
  `USE_SPEED_FOR_EXTENSION`(의미 변경: "ETA를 보낼지"), `REMAINING_TIME_THRESHOLD_SEC` /
  `MAX_TOTAL_EXTENSION_SEC` / `ETA_SAFETY_MARGIN`(**아두이노가 쓰는 값**으로 이관, 근거 보존용).

- **아두이노 쪽 필수 주의사항**: 파이가 같은 `EXTEND`를 재전송하므로 받을 때마다 더하면
  녹색이 무한히 늘어난다. **하강 구간당 한 번만** 적용하는 플래그가 반드시 필요하다.


### 2026-08-19: 연장 발급 단위를 "프레임"이 아니라 "잔여시간 하강 구간"으로 (엣지 트리거)

- **결정값**: `SignalExtensionStateMachine`이 `_armed` 플래그를 두고, **잔여시간이 임계값
  아래로 내려온 구간당 연장을 한 번만** 발급한다. 무장은 '연장 발급'으로만 소모되고,
  잔여시간이 임계값 위로 회복되면 되살아난다. 상태에 `SignalState.EXTENDED`(발급 후
  제어부 반영 대기)를 추가했다.

- **근거**: `evaluate()`는 실시간 루프에서 **매 프레임** 호출되는데, 원래 구현은 호출될
  때마다 조건이 참이면 `accumulated_extension_sec += step`을 했다. 즉 설계 의도인
  "매 사이클 재평가"가 코드에서는 "매 프레임 재연장"이 되어 있었다.

  10fps 기준 재현 결과(임계값 5초, 3번 구역 5초, 상한 15초, 사람이 계속 서 있음):

  | | 프레임별 발급값 | 결과 |
  |---|---|---|
  | 수정 전 | `[5, 5, 5, 0, 0, ...]` | **0.3초 만에 상한 소진**, 제어부로 명령 3연타 |
  | 수정 후 | `[5, 0, 0, 0, 0, ...]` | 하강 구간당 1회, 명령 1회 |

  제어부가 명령을 누적 처리하면 의도의 3배로 연장되고, 마지막 값만 반영하면 상한만
  헛되이 소진된다. 어느 쪽이든 **차량 신호 충돌 방지용 상한(`MAX_TOTAL_EXTENSION_SEC`)이
  안전장치로 기능하지 못한다.**

  무장을 '하강'이 아니라 '발급'으로 소모하는 이유: 임계값 아래로 내려간 뒤에 뒤늦게
  보행자가 들어오는 경우를 놓치지 않기 위해서다. 그리고 연장이 반영되어 잔여시간이
  회복되면 자동 재무장되므로, **느린 보행자가 한 사이클에서 두 번 연장받는 경로는
  그대로 살아 있다.** 그 총합을 막는 것이 상한의 역할이다.

  타임라인 검증(임계값 5초 / 3번 구역 5초 / 상한 15초):
  ```
   t   잔여  발급  누적   상태
   3     5     5     5   EXTENDING  <-- 연장
   8     5     5    10   EXTENDING  <-- 연장 (회복 후 재무장됐으므로)
  13     5     5    15   EXTENDING  <-- 연장
  18     5     0    15   CAPPED     (상한 도달)
  ```

- **함께 고친 것 — 사이클 경계**: `reset()`이 정의만 되어 있고 **아무 데서도 호출되지
  않았다.** 그대로 두면 첫 사이클에서 상한을 찍은 뒤 모든 사이클에서 영구히 `CAPPED`가
  된다(엣지 트리거 도입 전에는 0.3초 만에 그 상태가 됐다). `SignalExtensionPipeline.
  begin_new_cycle()`을 만들어 누적 연장·잔류 카운트·속도 히스토리를 함께 초기화한다.

  다만 **호출 주체는 제어부다.** 신호 사이클의 소유자가 제어부이므로 "새 녹색 시작"
  이벤트가 시리얼 프로토콜에 있어야 한다(`SerialComm.read_cycle_started()`).
  파이가 잔여 시간의 증감만 보고 추측하는 방식은 채택하지 않았다 — "시간이 갑자기 늘었다"가
  새 사이클인지 방금 요청한 연장이 반영된 것인지 구분할 수 없기 때문이다.

- **참고 자료/벤치마크**: 회귀 테스트
  `tests/test_signal_extend.py::test_extends_only_once_per_descent_below_threshold`,
  `::test_rearms_after_remaining_time_recovers`,
  `::test_arming_is_consumed_by_granting_not_by_descending`,
  `tests/test_pipeline.py::test_does_not_resend_extension_every_frame`,
  `::test_begin_new_cycle_resets_extension_budget`.
  상세 경위는 `docs/bugfix_log.md` A·B 항목.

  기존 테스트 `test_respects_upper_cap`은 **버그 동작을 그대로 인코딩**하고 있어서
  (연속 두 번 호출로 상한 도달) 함께 고쳤다. 통과하는 테스트가 잘못된 동작을 지켜주고
  있던 사례다.

- **관련 config 값**: `REMAINING_TIME_THRESHOLD_SEC`, `MAX_TOTAL_EXTENSION_SEC`,
  `ZONE_EXTENSION_SEC`, `SERIAL_MESSAGE_FORMAT`(사이클 시작 이벤트 포함 필요).

---

### 2026-08-19: 검출 모델을 yolov8n-pose로 교체 (목표 1·2가 추론 1회를 공유)

- **결정값**: `DETECTION_MODEL_PATH = "yolov8n-pose.pt"`,
  `DETECTION_CONFIDENCE_THRESHOLD = 0.4`(0.5에서 하향), `DETECTION_IMGSZ = 640`(신설).

- **근거**:

  **① 포즈 모델 하나가 박스와 키포인트를 동시에 준다.** 목표 1(신호 연장)은 사람 박스가,
  목표 2(쓰러짐 감지)는 몸통 각도를 재기 위한 키포인트가 필요하다. 모델을 따로 두면
  **추론이 2배**가 되는데, 실측상 추론이 프레임 시간의 사실상 전부다
  (비전 이후 전 단계 25µs vs YOLO 110ms, 약 4,400:1 — 이 PC 기준).

  실제 검증(사람 4명이 있는 이미지, `PersonDetector` 전 경로):
  ```
  PersonDetector.detect() -> 4명, 120 ms
    id=1 conf=0.89 keypoints=(17,3) 몸통각도=1.1
    id=2 conf=0.88 keypoints=(17,3) 몸통각도=1.7
    id=3 conf=0.85 keypoints=(17,3) 몸통각도=None   <- 키포인트 신뢰도 낮음 -> 비율 폴백
    id=4 conf=0.61 keypoints=(17,3) 몸통각도=None
  ```
  클래스는 `{0: "person"}` 하나뿐이라 `PEDESTRIAN_LABEL = "person"`이 그대로 맞는다.
  서 있는 사람의 몸통 각도가 1~2도로 나와 `fall_angle_deg = 50` 기준과 잘 분리된다.

  **② 신뢰도 임계값 0.4 — 두 목표가 추론을 공유하면 임계값도 하나뿐이다.**
  팀원 PoC는 0.4, 목표 1은 0.5였다. 낮은 쪽을 택한 이유: 넘어지는 순간 bbox가 급변하고
  신뢰도가 떨어져 검출이 통째로 빠지는 구간이 생기는데(팀원이 UR Fall 실측에서 확인,
  `FallTracker` 주석 참고), **쓰러진 사람을 놓치는 것이 연장 오탐보다 훨씬 위험하다.**
  목표 1의 사람 모형 관측치가 80%대였으므로 0.4도 여유가 충분하다.

  **③ `DETECTION_IMGSZ` 신설 — 파이가 느릴 때 가장 먼저 건드릴 값.**
  PoC에는 있었으나 우리 config에는 없어 ultralytics 기본값 640으로 돌고 있었다.
  측정 결과 `imgsz=320`이 2.8배 빠르고(110.5ms → 39.0ms, 이 PC 기준),
  **결과 좌표는 원본 프레임 기준으로 돌아온다**(`orig_shape=(480,640)` 확인).
  즉 이 값을 낮춰도 zone 좌표·호모그래피가 그대로 유효해 **재캘리브레이션이 필요 없다.**
  `CAMERA_RESOLUTION`을 낮추는 것과 결정적으로 다른 점이다.

- **참고 자료/벤치마크**: 위 수치는 전부 이 PC(개발용) 기준이다.
  **라즈베리파이 5 실측 FPS는 아직 미측정** — `tools/manual_camera_person_check.py`의
  FPS 표시로 확인할 것. 포즈 모델은 일반 검출 모델보다 헤드가 하나 더 붙으므로
  기존 `yolov8n.pt` 대비 느려질 수 있고, 그 차이도 파이에서 재야 한다.

- **관련 config 값**: `DETECTION_MODEL_PATH`, `DETECTION_CONFIDENCE_THRESHOLD`,
  `DETECTION_IMGSZ`, `DETECTION_TRACKER`, `PEDESTRIAN_LABEL`.

- **되돌리는 법**: `DETECTION_MODEL_PATH`를 `"yolov8n.pt"`로 되돌리면 목표 1은 그대로
  동작하고, 목표 2는 키포인트 없이 bbox 가로/세로 비율 폴백으로 동작한다(원문 그대로).
  코드 수정 없이 config 한 줄이다.

- **이전 결정과의 관계**: 2026-08-05 "검출 모델을 COCO 사전학습 yolov8n으로 확정"을
  **대체한다.** 그때 근거였던 "사람 모형이 80%대로 검출됨"은 포즈 모델에도 유효하나,
  파이 FPS는 다시 재야 한다.

---

### 2026-08-19: 연장 시간·상한을 국내 스마트횡단보도 운영 사례 기준으로 결정

- **결정값**:
  | config | 값 | 근거 유형 |
  |---|---|---|
  | `ZONE_EXTENSION_SEC[3]` (정중앙) | **5초** | 외부 운영 사례 (직접) |
  | `ZONE_EXTENSION_SEC[2]`, `[4]` | **3초** | 외부 운영 사례 (하한 채택) |
  | `ZONE_EXTENSION_SEC[1]`, `[5]` (양 끝) | **0초** | 설계상 확정 규칙 |
  | `MAX_TOTAL_EXTENSION_SEC` | **10초** | 외부 운영 사례 (직접) |
  | `REMAINING_TIME_THRESHOLD_SEC` | **5초** | 설계 논리 (외부 근거 아님) |

- **근거**:

  **① 3번 구역 5초 — 국내 동일 구성 사례에서 직접 가져왔다.**
  광주광역시가 2024년 7월부터 운영하는 스마트횡단보도는 **"신호등 기둥에 부착한 인공지능
  카메라가 보행자를 감지"**해 길을 건널 시간이 부족하다고 판단되면 **"최대 5초간 자동으로
  연장"**한다. 충북 음성군 사례도 **"5초 간격으로 연장"**한다.
  이 두 사례는 센서 위치(신호등 기둥 부착 카메라), 판단 방식(영상 기반 잔류 감지),
  목적(교통약자 보호)이 **우리 프로젝트와 사실상 동일**하다. 임의로 정할 이유가 없어
  그대로 채택했다.

  **② 누적 상한 10초 — 강진군 시스템의 허용 범위 상한.**
  강진군 보행신호 자동연장 시스템은 "보행신호 시간 내에 도로를 횡단하지 못한 보행자를
  인식해 **허용된 시간(5~10초) 범위 내에서** 보행신호 시간을 자동으로 연장"한다.
  이 범위의 상한을 한 사이클 누적 상한으로 삼았다. 차량 신호와의 충돌을 막는 안전장치이므로
  실제로 운영 중인 시스템이 넘지 않는 값을 쓰는 것이 타당하다.

  **③ 2·4번 구역 3초 — 서울시 연장 폭의 하한.**
  서울시·서울경찰청은 고령자 비율이 높고 사고가 잦은 123개소를 선정해
  **"횡단보도 길이에 따라 3초에서 최대 6초까지 보행신호시간을 연장"**했다.
  2·4번은 중앙보다 남은 거리가 짧으므로 이 범위의 최소 단위인 3초를 쓴다.
  (3번 5초와 함께 두면 3 < 5 < 6 으로 서울시 범위 안에 자연스럽게 들어간다.)

  **④ 트리거 임계값 5초 — 이것만은 외부 근거가 아니라 설계 논리다.**
  1회 최대 연장량(5초)과 같게 뒀다. 남은 시간이 5초를 넘으면 연장 한 번으로 메울 수 있는
  범위 밖이고, 시간이 더 줄어든 뒤 다시 판단할 기회가 오기 때문이다(엣지 트리거 —
  `docs/bugfix_log.md` A 항목). **외부 규정에서 가져온 값이 아니므로 팀 확인이 필요하다.**

- **참고 자료/벤치마크**:
  - [‘보행신호등 자동 연장’… 광주시, 스마트횡단보도 운영 (서울신문, 2024-07-01)](https://www.seoul.co.kr/news/society/2024/07/01/20240701500085)
    — "신호등 기둥에 부착한 인공지능 카메라가 보행자를 감지", "최대 5초간 자동으로 연장",
    노인·어린이 교통사고 위험지역 23개소, 국토교통부 ITS 구축 공모 선정 사업(약 19억원).
  - ["건널 시간 부족하면 신호 자동 연장"… 보행자 지키는 똑똑한 스마트횡단보도 (한국일보, 2025-04-23)](https://www.hankookilbo.com/News/Read/A2025042314080004259)
    — 충북 음성군, "신호 시간을 5초 간격으로 연장", 초중고 밀집 지역 10곳.
  - [강진군, 보행신호 자동연장 시스템 도입한다 (전남일보)](https://www.jnnews.co.kr/m/view.php?idx=374679)
    — "허용된 시간(5~10초) 범위 내에서 보행신호 시간을 자동으로 연장".
  - [교통약자 누구나! 천천히 안심하고 건너세요… 서울시·서울경찰청, 횡단보도 보행신호 시간 연장 확대 (서울시 공식)](https://news.seoul.go.kr/traffic/archives/512963)
    — "횡단보도 길이에 따라 3초에서 최대 6초까지 보행신호시간을 연장", 123개소 선정.
  - [용인특례시, 보행 신호 알아서 늘려주는 스마트 횡단보도 설치 (용인시 공식)](https://tv.yongin.go.kr/m/vod.asp?code=&ccode=010002000000&vod_id=2024042391453509)
    — 신호등 기둥 AI 카메라 기반 '보행 신호 자동 연장시스템', 2022년 시범 운영 후 2024년 14개소 확대.

- **관련 config 값**: `ZONE_EXTENSION_SEC`, `MAX_TOTAL_EXTENSION_SEC`,
  `REMAINING_TIME_THRESHOLD_SEC`.

- **남은 확인 사항**: 위 값들은 **실제 도로 기준**이다. 우리 것은 축소 모형이라
  보행 거리와 실제 보행 시간이 다르므로, 모형에서 사람 모형을 실제로 움직여 보고
  "5초 연장이 모형 스케일에서 의미 있는 양인가"를 확인해야 한다. 값이 과하면
  모형 비율에 맞춰 축소하되, **그 경우에도 비율(3 : 5 : 상한 10)은 유지**하는 것이
  근거를 설명하기 좋다.

---

### 2026-08-19: 속도 추정을 연장 시간 결정에 반영 (ETA 기반 차감)

- **결정값**: `USE_SPEED_FOR_EXTENSION`을 켜면 구역별 연장 시간이 **상한**이 되고,
  실제 연장은 그 사람이 정말 모자란 만큼으로 줄어든다. `ETA_SAFETY_MARGIN = 1.25`.

  ```
  사람마다:  필요량ᵢ = min( ZONE_EXTENSION_SEC[구역ᵢ],  ceil(ETAᵢ x 1.25 - 잔여시간) )
  전체:      연장 = max(필요량ᵢ)          # 0 이하면 연장하지 않음
  ```

- **근거**:

  **① 비교 대상은 ETA가 아니라 '부족분'이다.** 판단 시점에 보행자에게는 이미 잔여시간이
  있으므로, ETA가 7초여도 5초가 남아 있으면 실제로 필요한 건 2초다. 구역 규칙만 쓰면
  이 경우에도 5초를 줘서 **차량에서 3초를 헛되이 뺏는다.**

  **② 속도는 연장을 '깎기만' 한다.** 구역 값이 상한으로 남으므로
  "양 끝 구역은 연장하지 않는다"(CLAUDE.md 2.4)는 설계 규칙이 그대로 지켜진다.
  1번 구역에 있는 사람의 ETA가 20초여도 `min(0, ...) = 0`이다.

  **③ 안전계수 1.25 — 경찰청 보행속도 기준의 교통약자 보정 비율과 같다.**
  국내 보행신호시간은 **"보행진입시간 7초 + 횡단보도 1m당 1초"**(보행속도 1.0m/s)로
  산정하는데, 교통약자가 많은 지역에서는 **"0.8m당 1초"**(0.8m/s)로 완화한다.
  그 완화 비율이 `1.0 / 0.8 = 1.25`다. 우리도 측정된 ETA를 낙관적으로 믿지 않고
  같은 비율만큼 부풀린 뒤 올림한다.

  이 여유가 필요한 이유는 **위험이 비대칭**이기 때문이다. 과대 연장은 차량이 조금 더
  기다리는 것이지만, 과소 연장은 **보행자가 도로에 갇히는 것**이다. 실제로 노인의
  15th percentile 보행속도가 0.85m/s로 측정된 연구가 있어, 기준 속도보다 느린 보행자가
  실재한다는 것도 확인된다.

  **④ 여러 명이면 사람마다 계산 후 최댓값.** 속도를 넣는 순간 "가운데 있는 사람이 가장
  오래 걸린다"는 가정이 깨진다. 3번 구역 사람이 빨라서 1초만 필요하고 2번 구역 사람이
  느려서 3초가 필요한 상황에서, 가운데만 보면 **느린 쪽을 도로에 가둔다.**
  회귀 테스트 `test_multiple_pedestrians_use_per_person_need_not_center_only`가 이 케이스다.

  **⑤ 폴백이 안전한 방향이다.** ETA가 `None`이면(호모그래피 없음/정지/샘플 부족)
  구역 규칙 전체를 준다. 특히 "사람이 멈췄다"는 시간이 더 필요한 상황이므로 이게 옳다.

- **참고 자료/벤치마크**:
  - ['신호등 속 과학' 횡단보도 보행신호시간 어떻게 정해지나 (서울시 공식)](https://news.seoul.go.kr/traffic/archives/12356)
    — **"보행진입시간 '7초' + 횡단보도 '1m 당 1초'"**를 원칙으로 하며, 교통약자가 많은
    장소에는 **"0.8m 당 1초"**의 완화된 기준을 적용. → 안전계수 1.25(= 1.0/0.8)의 근거.
  - [교통신호기 설치·운영 업무편람 (개정 2023-01-17, 도로교통공단)](https://www.koroad.or.kr/main/board/21/87215/board_view.do?cp=1&listType=list&bdOpenYn=Y&bdNoticeYn=N)
    — 보행신호시간 산정의 근거 문서. 원문 확인 후 인용 보강 필요.
  - [「교통신호기 설치·운영 업무편람」 개정 알림 (경찰청)](https://www.police.go.kr/user/bbs/BD_selectBbs.do?q_bbsCode=1001&q_bbscttSn=20230117161850418)
    — 경찰청 원 게시물. 2020-11-30 개정으로 교통약자를 고려해 횡단보도 보행속도 기준 조정.
  - [노인보호구역 보행자녹색시간 산정을 위한 보행속도 기준 개선 (KCI 등재 논문)](https://www.kci.go.kr/kciportal/landing/article.kci?arti_id=ART002621242)
    — 노인의 15th percentile 보행속도 0.85m/s로 분석. 기준 속도보다 느린 보행자가
    실재한다는 근거 → 안전계수를 두는 이유.
  - [스마트횡단시스템 활성화를 위한 교통약자의 횡단속도 추정 (KISTI)](https://accesson.kisti.re.kr/archive/pdfDown.do?arti_id=ATN0041557988)
    — 스마트횡단시스템 맥락의 교통약자 횡단속도 연구. 속도 기반 판단의 선행 연구.

- **관련 config 값**: `USE_SPEED_FOR_EXTENSION`(기본 `False`), `ETA_SAFETY_MARGIN`,
  `CROSSWALK_REAL_WIDTH_CM` / `CROSSWALK_REAL_LENGTH_CM`(**ETA의 전제 — 없으면 기능이
  자동으로 꺼진 것과 같아진다**), `SPEED_WINDOW_SEC`.

- **켜기 전 확인 사항**: 기본값을 `False`로 둔 이유는 ETA가 속도 추정 정확도에 직결되기
  때문이다. 순서대로 확인할 것.
  1. `tools/zone_calibrator.py`를 `--width-cm` / `--length-cm`와 함께 실행 (호모그래피 생성)
  2. `tools/manual_camera_person_check.py`에서 `unit`이 `cm/s`로 뜨는지 확인
  3. 사람 모형을 **일정한 속도로** 움직이며 표시되는 속도·ETA가 실제와 맞는지 확인
     (오차가 크면 `SPEED_WINDOW_SEC` 조정)
  4. 그다음 `USE_SPEED_FOR_EXTENSION = True`

---

### 2026-08-05: 구역 5등분을 화면(픽셀)이 아니라 실제 거리(cm) 기준으로 수행

- **결정값**: 호모그래피가 있으면 평면 좌표(cm)에서 균등 분할 후 픽셀로 역변환
  (`CrosswalkZones._split_on_ground`). 실측 치수가 없을 때만 화면상 분할로 대체.
- **근거**: 사선 구도에서 화면상 균등 분할은 실제 거리로 균등하지 않다. 먼 쪽 1픽셀이
  가까운 쪽 1픽셀보다 훨씬 긴 실거리를 나타내기 때문이다.
  폭 400cm × 길이 1000cm 횡단보도를 사선으로 본 좌표
  `[(80,460), (560,460), (400,150), (240,150)]` 로 측정한 결과:

  | 분할 방식 | 1번 | 2번 | 3번 | 4번 | 5번 |
  |---|---|---|---|---|---|
  | 화면(픽셀) 기준 | 77cm | 105cm | 152cm | 238cm | 429cm |
  | 실거리(cm) 기준 | 200cm | 200cm | 200cm | 200cm | 200cm |

  화면 기준으로는 1번과 5번이 **5.6배** 차이났고, **횡단보도의 실제 한가운데(500cm)가
  3번이 아니라 4번 구역**에 들어갔다. 이는 "정중앙이면 가장 길게 연장"(CLAUDE.md 2.4)이라는
  설계 전제를 무효화한다.
- **참고 자료/벤치마크**: 사영변환은 직선을 직선으로 보내므로, 평면상 직사각형 띠의 네 꼭짓점만
  역변환하면 화면상 정확한 구역 사각형이 된다(근사가 아님). 회귀 방지 테스트는
  `tests/test_zone.py::test_zones_are_equal_in_real_distance_with_dimensions` 및
  `::test_true_center_of_crosswalk_lands_in_middle_zone`.
- **관련 config 값**: `CROSSWALK_REAL_WIDTH_CM`, `CROSSWALK_REAL_LENGTH_CM`
  (미입력이면 대체 경로로 떨어져 위 왜곡이 발생하므로 실측 우선순위가 높다),
  `CROSSWALK_ZONE_COUNT`.

### 2026-08-05: 라즈베리파이 CSI 카메라 접근 경로를 Picamera2로 확정

- **결정값**: `src/capture.py`에Picamera2 백엔드 추가, `config.CAMERA_SOURCE = "picamera2"` 로 선택.
- **근거**: 하드웨어가 라즈베리파이 5 + CSI 리본 카메라로 확정됐다. 파이 5는 Raspberry Pi OS
  Bookworm 이상만 지원하고 Bookworm에는 레거시 카메라 스택(`bcm2835-v4l2`)이 없어
  CSI 카메라가 `/dev/video0`으로 잡히지 않는다. `cv2.VideoCapture(0)`은 실패한다.
  담당자가 파이에서 `tools/manual_rpicam_person_check.py`( Picamera2 직접 호출)로 정상 동작을 확인했다.
- **참고 자료/벤치마크**: Picamera2의 `format="RGB888"`은 이름과 달리 메모리상 BGR 순서 배열을
  반환한다(libcamera 명명 규칙). 덕분에 cv2 백엔드와 형식이 일치해 하위 단계가 카메라 종류를
  몰라도 된다. 라즈베리파이 실측 FPS는 아직 미측정.
- **관련 config 값**: `CAMERA_SOURCE`, `CAMERA_RESOLUTION`.

### 2026-08-05: 검출 모델을 COCO 사전학습 yolov8n으로 확정

- **결정값**: `DETECTION_MODEL_PATH = "yolov8n.pt"`, `PEDESTRIAN_LABEL = "person"`,
  `DETECTION_CONFIDENCE_THRESHOLD = 0.5`.
- **근거**: 담당자가 카메라로 실측한 결과 COCO 사전학습 yolov8n이 사람 모형을 `person`으로
  검출했고 신뢰도가 80%대로 관측됐다. 추가 파인튜닝이 필요 없다고 판단. 임계값 0.5는 관측치
  80%대 대비 여유가 있는 값이며, 오탐 발생 시 재조정한다.
- **참고 자료/벤치마크**: 라즈베리파이 실측 FPS는 아직 미측정 —
  `tools/manual_camera_person_check.py` 화면 좌상단 FPS 값으로 확인할 것.
- **관련 config 값**: `DETECTION_MODEL_PATH`, `PEDESTRIAN_LABEL`,
  `DETECTION_CONFIDENCE_THRESHOLD`, `DETECTION_TRACKER`.
