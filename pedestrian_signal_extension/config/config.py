"""프로젝트 전역 설정.

팀과 아직 확정되지 않았거나 담당자가 임의로 정한(=근거 없는) 값은 None 또는 명확한
placeholder로 두고, 실제 사용 지점에서 값이 None이면 에러를 내도록 한다
(임의 추정치로 몰래 동작하지 않게 하기 위함).
CLAUDE.md 6장 "팀과 논의가 필요한 미확정 사항" 참고.
"""

# =====================================================================
# 위치 기반(구역별) 신호 연장 — 담당 파트(목표 1) 핵심 설계
# =====================================================================
# 횡단보도를 "걷는 방향"을 따라 위치 순서대로 5개 구역으로 나눈다.
#
#   진입 끝 ┃ 1 │ 2 │ 3 │ 4 │ 5 ┃ 반대 끝
#           └───┴───┴───┴───┴───┘
#   1, 5 = 양 끝(시작) 구역   2, 4 = 끝과 중앙 사이 구역   3 = 정중앙 구역
#
# 남은 신호 시간이 임계값 이하일 때, 보행자가 "어느 구역에 있는지"에 따라 연장 시간을 차등한다.
# (가운데에 있을수록 아직 더 건너야 하므로 더 길게 연장.)
CROSSWALK_ZONE_COUNT = 5

# 구역 번호 -> 연장 시간(초).
#
# 이 값들은 담당자 임의값이 아니라 **국내 스마트횡단보도 실사례**를 근거로 정했다
# (docs/decisions.md "2026-08-19: 연장 시간·상한을 국내 운영 사례 기준으로 결정" 참고).
#   - 3번(정중앙) 5초: 광주시 스마트횡단보도가 AI 카메라로 잔류 보행자를 감지해 "최대 5초"
#     자동 연장하고, 충북 음성군도 "5초 간격"으로 연장한다. 우리와 구성(신호등 기둥 부착
#     카메라 + 영상 판단)이 사실상 같은 사례라 그대로 채택했다.
#   - 2·4번 3초: 서울시가 횡단보도 길이에 따라 보행신호를 "3~6초" 연장한 사례의 하한.
#     중앙보다 덜 남았으므로 최소 단위를 쓴다.
# 1/5번(양 끝)은 "연장하지 않는다"가 설계상 확정 규칙이므로 0.
# None인 구역에 보행자가 있으면 사용 지점에서 NotImplementedError가 난다.
ZONE_EXTENSION_SEC = {
    1: 0,     # 양 끝 구역 — 연장 안 함 (거의 다 건넜거나 이제 막 진입)
    2: 3,     # 끝-중앙 사이. 근거: 서울시 3~6초 연장 사례의 하한.
    3: 5,     # 정중앙. 근거: 광주시 "최대 5초", 음성군 "5초 간격" 자동 연장.
    4: 3,     # 중앙-끝 사이. 2번과 대칭.
    5: 0,     # 양 끝 구역 — 연장 안 함
}

# 연장 트리거를 발동할 "남은 신호 시간" 임계값(초). 이 값 이하로 남았을 때만 위 규칙을 적용.
#
# 근거(외부 규정이 아니라 설계 논리): 1회 최대 연장량(5초)과 같게 둔다. 남은 시간이 5초보다
# 많으면 연장 한 번으로 메울 수 있는 범위 밖이고, 어차피 시간이 더 줄어든 뒤에 다시 판단할
# 기회가 온다(엣지 트리거 — src/signal_extend.py). 팀 확인 대상.
REMAINING_TIME_THRESHOLD_SEC = 5

# 연장 누적 상한(초). 차량 신호와의 충돌 방지를 위한 안전 상한.
# 근거: 강진군 보행신호 자동연장 시스템이 "허용된 시간(5~10초) 범위 내에서" 연장한다는
# 운영 사례의 상한값. 즉 한 사이클에서 최대 10초까지만 늘린다 (docs/decisions.md 참고).
MAX_TOTAL_EXTENSION_SEC = 10

# --- 교통약자(휠체어/목발 등) 우선 연장 — 현재 보류 (CLAUDE.md 2.5) ---
# 검출 수단이 없어(MOBILITY_AID_LABELS = ()) priority_mode가 항상 False이므로 아래 값은 지금 쓰이지 않는다.
# 파인튜닝 모델이 생기면 재개한다. 설계 의도는 아래 그대로 유지:
# "처음부터 시간을 넉넉하게 준다"는 부분은 제어부가 기본 녹색시간을 늘리는 방식으로 구현하고
# (파이 -> 제어부로 priority 플래그 전달), 파이 쪽 연장 규칙은 아래 값으로 차등한다.
# 미정이면 None으로 두고, 이 경우 state machine은 일반 규칙(ZONE_EXTENSION_SEC)으로 대체 동작한다.
PRIORITY_ZONE_EXTENSION_SEC = None       # TODO(팀 확정 필요): 우선 연장 구역별 차등 맵 {1:.., 2:.., ...}.
PRIORITY_MAX_TOTAL_EXTENSION_SEC = None   # TODO(팀 확정 필요): 우선 연장 시 별도(더 높은) 상한.

# --- Zone 잔류/확정 판단 (CLAUDE.md 2.2) ---
# 검출 흔들림에 대비해, 횡단보도(아무 구역) 안에서 이만큼 연속 검출돼야 "확정 보행자"로 본다.
ZONE_RESIDENCY_FRAMES = None    # TODO(실측 필요): 실측 FPS 기반으로 튜닝.
ZONE_CONFIG_PATH = "data/zone_config.json"  # tools/zone_calibrator.py 로 생성되는 zone 좌표 파일 경로.

# =====================================================================
# 횡단보도 평면 좌표 변환(호모그래피) — 속도 추정용 (CLAUDE.md 2.2, 2.3)
# =====================================================================
# 카메라가 횡단보도를 "사선"으로 비추므로 픽셀 거리와 실거리의 비율이 화면 위치마다 다르다
# (가까운 쪽 1px < 먼 쪽 1px). 보행자의 발은 지면(평면) 위에 있으므로, 평면 하나에 대한
# 사영변환(호모그래피)으로 픽셀 좌표를 횡단보도 평면 좌표(cm)로 펼 수 있다.
#
# 별도의 체스보드 카메라 캘리브레이션은 필요 없다. tools/zone_calibrator.py 에서 이미 찍는
# 네 꼭짓점 + 아래 실측 치수만 있으면 cv2.getPerspectiveTransform 으로 변환 행렬이 나온다.
#
# 평면 좌표계 규약:  y = 걷는 방향(0 -> LENGTH),  x = 폭 방향(0 -> WIDTH)
#
# 두 값이 모두 채워져 있어야 GroundPlane이 만들어진다. 하나라도 None이면
# CrosswalkZones.ground_plane 이 None이 되고, 구역 판정은 그대로 동작하되
# 속도는 px/s로만 나온다(원근 보정 없이 cm인 척하지 않기 위함).
CROSSWALK_REAL_WIDTH_CM = None   # TODO(실측 필요): 횡단보도(모형) 폭 — 걷는 방향과 수직인 변의 길이.
CROSSWALK_REAL_LENGTH_CM = None  # TODO(실측 필요): 횡단보도(모형) 길이 — 걷는 방향(시작 변 -> 끝 변)의 길이.

# =====================================================================
# 속도 추정 (CLAUDE.md 2.3)
# =====================================================================
# 속도는 "최근 SPEED_WINDOW_SEC초 동안의 변위 / 경과 시간"으로 낸다.
# 한 프레임 차분은 검출 박스 지터에 그대로 흔들리므로 쓰지 않는다.
#   - 너무 짧으면(예: 0.1초) 노이즈가 크고, 너무 길면(예: 2초) 속도 변화 반응이 느려진다.
#   - 0.5는 보행 주기(대략 1보 0.5초 안팎)를 한 번 담을 정도의 출발점일 뿐, 실측 후 조정할 것.
SPEED_WINDOW_SEC = 0.5

# 위 윈도우 안에 최소 이만큼 샘플이 쌓여야 속도를 낸다(그 전엔 None 반환).
SPEED_MIN_SAMPLES = 2

# 계산된 속도를 신호 연장 시간 결정에 반영할지 여부.
#
# True로 켜면 구역별 연장 시간이 '상한'이 되고, 실제 연장은 그 사람이 정말 모자란 만큼으로 줄어든다:
#
#     부족분 = 예상 통과 시간(ETA) x ETA_SAFETY_MARGIN - 남은 신호 시간
#     연장   = min( ZONE_EXTENSION_SEC[구역], ceil(부족분) )      # 부족분 <= 0 이면 연장 안 함
#
# 즉 속도는 연장을 '깎기만' 하고 늘리지 않는다 — "양 끝 구역은 연장 안 함" 같은 구역 정책이
# 그대로 상한으로 남는다. 여러 명이면 사람마다 따로 계산해 가장 큰 값을 쓴다
# (가운데 사람이 항상 가장 오래 걸린다는 보장이 없어지므로).
#
# 전제: ETA는 호모그래피가 있어야 나온다. CROSSWALK_REAL_*_CM 이 비어 있으면 속도가 px/s로
# 떨어져 ETA가 None이 되고, 그러면 자동으로 구역 규칙만 쓰는 동작으로 되돌아간다(안전한 폴백).
#
# 기본값 False — 켜기 전에 (1) zone 캘리브레이션 + 실측 치수 입력, (2) 사람 모형을 일정 속도로
# 움직여 SpeedEstimator 출력이 실제와 맞는지 확인이 끝나야 한다 (CLAUDE.md 3장 6번).
USE_SPEED_FOR_EXTENSION = False

# ETA에 곱하는 안전계수. 속도 추정이 실제보다 빠르게 나오면 연장을 덜 주게 되는데,
# 과소 연장은 '보행자가 도로에 갇히는' 결과라 과대 연장(차량 대기)보다 훨씬 위험하다.
# 그래서 ETA를 낙관적으로 믿지 않고 이 계수만큼 부풀린 뒤 올림한다.
#
# 근거: 경찰청 보행신호시간 산정에서 일반 보행속도 1.0m/s를 보행약자 지역에서 0.8m/s로
# 완화하는데, 그 비율이 1.0/0.8 = 1.25다. 국내 기준이 교통약자를 위해 잡아 두는 여유와
# 같은 비율을 쓴다 (docs/decisions.md 참고).
#
# USE_SPEED_FOR_EXTENSION이 True인데 이 값이 None이면 생성 시점에 NotImplementedError가 난다.
ETA_SAFETY_MARGIN = 1.25

# =====================================================================
# 검출 모델 (CLAUDE.md 2.6)
# =====================================================================
# 실측 확인 결과 COCO 사전학습 yolov8n이 사람 모형을 "person"으로 검출했고 신뢰도는 80%대였다.
# -> 추가 파인튜닝 없이 사전학습 가중치를 그대로 쓴다.
PEDESTRIAN_LABEL = "person"     # COCO 클래스명. 파인튜닝 모델로 교체하면 그 클래스명으로 바꾼다.

# 교통약자(휠체어/목발/지팡이)는 현재 "보류" (CLAUDE.md 2.5).
# COCO에 해당 클래스가 없어 검출 수단 자체가 없으므로 빈 튜플로 두어 priority_mode가 항상 False가 되게 한다.
# 파인튜닝 모델이 생기면 ("wheelchair", "cane", "crutches") 처럼 학습 클래스명을 채운다.
MOBILITY_AID_LABELS = ()

DETECTION_MODEL_PATH = "yolov8n.pt"   # ultralytics가 없으면 최초 실행 시 자동 다운로드(약 6MB).
DETECTION_CONFIDENCE_THRESHOLD = 0.5  # 사람 모형 관측치 80%대 대비 여유 있는 값. 오탐 발생 시 조정.

# YOLO 추적기 설정. track_id가 있어야 SpeedEstimator와 CrosswalkOccupancy가 사람별로 구분할 수 있다.
DETECTION_TRACKER = "bytetrack.yaml"  # ultralytics 내장 추적기. 필요 시 "botsort.yaml"로 교체.

# =====================================================================
# 카메라 (CLAUDE.md 2.1)
# =====================================================================
# CameraCapture가 소스 종류를 보고 백엔드를 고른다. 아래 중 하나를 쓸 수 있다.
#   - "picamera2"           : 라즈베리파이 CSI 리본 카메라 (Picamera2/libcamera 경유)
#   - 정수 0, 1, ...        : 이 코드를 "실행하는 컴퓨터"에 직접 연결된 USB 웹캠 인덱스
#   - "path/to/video.mp4"   : 테스트용 동영상 파일
#   - "http://<파이IP>:8000/": 라즈베리파이가 tools/pi_camera_server.py 로 내보내는 MJPEG 스트림 URL
#                             (카메라는 파이에, 무거운 추론은 PC에서 돌리는 구성)
#
# 중요: 라즈베리파이 5 + Raspberry Pi OS Bookworm에는 레거시 카메라 스택이 없어서
# CSI 카메라가 /dev/video0으로 잡히지 않는다. 파이에서 CSI 카메라를 쓸 때는 반드시
# "picamera2"로 둘 것. 0으로 두면 cv2.VideoCapture가 실패한다.
CAMERA_SOURCE = 0

# 프레임 해상도 (width, height). 두 백엔드(cv2 / Picamera2)에 동일하게 적용된다.
#
# 중요: zone 좌표와 호모그래피는 "캘리브레이션 당시 해상도"에 종속된다. 이 값을 바꾸면
# data/zone_config.json이 통째로 무효가 되므로 재캘리브레이션해야 한다.
# 값을 정한 근거: tools/manual_rpicam_person_check.py 로 파이에서 동작을 확인할 때 쓴 크기와
# 같게 맞춰 둔 출발점이다. 파이 FPS가 부족하면 더 낮추고(연산량 감소), 그때 재캘리브레이션할 것.
CAMERA_RESOLUTION = (640, 480)

# 카메라는 보행자 신호등에 붙어 횡단보도를 "사선"으로 비춘다(탑뷰 아님).
# 이 각도는 캘리브레이션 결과(호모그래피)에 그대로 반영되므로 아래 값은 기록용이다.
# 주의: 카메라를 움직이면 zone 좌표와 호모그래피가 모두 무효 -> 재캘리브레이션 필요.
CAMERA_MOUNT_ANGLE_DEG = None   # TODO(실측 필요): 보행자 신호등 부착 각도(수평 기준 내려보는 각).

# =====================================================================
# 라즈베리파이 - 제어부(ESP32) 시리얼 통신 (CLAUDE.md 1, 2.5)
# =====================================================================
# 주의: 제어부 ESP32 / 구동부 아두이노 우노 구성은 잠정안이며 바뀔 수 있다.
# 그래서 아래 값은 전부 config로 분리해 두고, 보드가 바뀌어도 여기만 고치면 되게 한다.
SERIAL_PORT = None       # TODO(팀 합의 필요): 실제 포트 (예: "/dev/ttyUSB0", "/dev/ttyACM0", "COM3").
SERIAL_BAUDRATE = None   # TODO(팀 합의 필요): 보드레이트.
SERIAL_MESSAGE_FORMAT = None  # TODO(팀 합의 필요): 메시지 포맷 스펙 (프레이밍, 필드 정의 등).
